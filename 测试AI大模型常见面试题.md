# 测试 AI 大模型常见面试题总结

> 适用于 AI 测试 / 大模型质量保障 / 算法评测岗位面试准备

---

## 一、AI 大模型测试基础

### 1. AI 大模型测试和传统软件测试有什么区别？

| 对比项       | 传统软件测试                 | AI/大模型测试                        |
|--------------|------------------------------|--------------------------------------|
| 输出确定性   | 输入确定则输出预期可枚举     | 同一输入可有多种合理输出（非确定性） |
| 预期结果     | 多为唯一或有限种正确结果     | 需容忍语义等价、多表述、部分正确     |
| 评估方式     | 断言通过/失败                | 自动指标 + 人工打分 + 抽样复核       |
| 回归         | 用例稳定、结果可复现         | 模型/参数变化会导致结果分布变化      |
| 缺陷定义     | 与规格不符即缺陷             | 幻觉、偏见、不安全、不符合预期能力   |

**面试点**：大模型测试更强调「评估而非断言」「非确定性下的质量定义」「多维评估与人工兜底」。

---

### 2. 大模型测试主要关注哪些维度？

- **能力**：理解、生成、推理、知识、多轮对话、指令遵循等是否满足产品预期。
- **事实性与幻觉**：是否捏造事实、时间、数据；是否与给定上下文一致。
- **安全与合规**：敏感内容、越狱、偏见歧视、隐私、合规红线。
- **鲁棒性**：对 Prompt 表述变化、对抗输入、边界输入的稳定性。
- **性能与成本**：首 token 延迟、吞吐、Token 消耗与成本、资源占用。
- **一致性/可复现**：相同输入下行为是否合理稳定（在允许随机性的前提下）。

**面试点**：能力 + 幻觉 + 安全 + 鲁棒 + 性能 + 一致性，是常见测试维度。

---

### 3. 什么是「幻觉」？为什么大模型会产生幻觉？

- **幻觉（Hallucination）**：模型生成**看似合理但事实上错误或与给定信息不符**的内容，如虚构人物、错误日期、编造引用、与检索上下文矛盾等。
- **原因**：模型基于统计规律生成，没有真正的「事实核查」；训练数据噪声与错误；过拟合或泛化到错误模式；上下文过长导致「注意力」偏移；指令或上下文歧义。

**面试点**：幻觉 = 事实错误或与给定信息不符；原因可从数据、生成机制、上下文理解几方面说。

---

## 二、提示词（Prompt）与对话测试

### 4. 什么是 Prompt？测试中为什么要关注 Prompt？

- **Prompt**：用户或系统给模型的**输入文本**（含指令、上下文、示例等），决定模型「做什么、按什么格式、参考什么信息」。
- **测试关注**：同一能力下，不同 Prompt 表述会导致效果差异；需测试 **Prompt 鲁棒性**（换说法、加噪声、多语言）、**注入与越狱**（恶意指令注入）、**边界与超长输入**。

**面试点**：Prompt 决定行为；测试要覆盖表述变化、恶意输入、边界与长度。

---

### 5. 什么是 Prompt 注入？测试时如何覆盖？

- **Prompt 注入**：通过在用户输入中**嵌入特殊指令或格式**，试图覆盖或绕过系统预设的 Prompt（如角色设定、禁止事项），让模型执行本不该执行的操作。
- **测试**：构造「伪装成普通内容的指令」、多语言/编码绕过、长文中夹带指令、角色扮演类诱导等；验证模型是否仍遵守系统约束、不泄露系统 Prompt、不越权。

**面试点**：注入 = 用户输入篡改系统意图；测试要主动构造各种注入样本并验证防护。

---

### 6. 多轮对话测试关注什么？

- **上下文保持**：是否记住前文信息、指代消解是否正确。
- **角色与设定**：是否在长对话中保持人设与约束。
- **长度与截断**：轮次多、上下文过长时是否丢信息、是否截断合理。
- **话题切换与回归**：换话题后再回到原话题是否仍一致。
- **边界**：空回复、重复提问、无效输入时的表现。

**面试点**：上下文、角色一致性、长对话与截断、话题变化与边界。

---

## 三、评估指标与方法

### 7. 大模型常用的自动评估指标有哪些？

| 类型         | 指标示例           | 适用场景                 |
|--------------|--------------------|--------------------------|
| 文本匹配     | BLEU、ROUGE        | 翻译、摘要与参考译文/摘要对比 |
| 语义相似     | BERTScore、余弦相似度 | 语义等价、多表述        |
| 分类/选择    | 准确率、F1         | 选择题、分类、判断对错   |
| 逻辑/推理    | 准确率、步骤正确率 | 数学、推理链、代码执行结果 |
| 事实性       | 与知识库/检索结果对比、NLI 模型 | 减少幻觉、RAG 场景   |
| 专用基准     | 各 benchmark 得分  | 能力横向对比、回归       |

**面试点**：没有「万能指标」；按任务选（生成用 BLEU/ROUGE/语义，选择用准确率，事实性用检索或 NLI 辅助）。

---

### 8. 人工评估（Human Evaluation）为什么重要？怎么做？

- **重要性**：自动指标无法完全反映流畅度、有用性、安全性、主观体验；很多任务没有标准答案或答案多样。
- **做法**：制定**评分维度与标准**（如相关性、事实性、安全性、1～5 分）；**抽样**评估或对关键场景全量评估；**多人标注**与一致性检查（如 Kappa）；可结合 A/B 测试看用户行为与反馈。

**面试点**：自动指标 + 人工评估结合；人工要标准化、可复现、有一致性校验。

---

### 9. 什么是 Benchmark？你了解哪些大模型评测基准？

- **Benchmark**：标准化的**评测数据集 + 评测协议**，用于横向对比模型能力或做回归。
- **常见**：
  - **通用能力**：MMLU、C-Eval、GAOKAO — 多学科选择题。
  - **推理**：GSM8K、MATH — 数学；HumanEval、MBPP — 代码。
  - **中文**：C-Eval、CMMLU、Gaokao 等。
  - **安全/对齐**：TruthfulQA、安全性专项数据集等。
  - **长文本/多模态**：按任务有不同基准。

**面试点**：能说出 2～3 个与岗位相关的基准及其用途（如 C-Eval 综合、HumanEval 代码）。

---

## 四、幻觉与事实性测试

### 10. 如何测试和降低「幻觉」？

- **测试**：
  - **闭卷**：问事实类问题，与权威知识库/标准答案对比，统计错误与捏造比例。
  - **RAG/开卷**：给定检索片段，问相关问题，检查回答是否**只基于给定内容**、有无无依据扩展或篡改。
  - **边界**：问模型不知道或过新的事件，观察是否诚实说「不知道」还是瞎编。
- **降低**：RAG 约束「仅依据检索结果」、引用来源；Prompt 要求「不确定则说明」；后处理校验（NLI、与知识库比对）；模型层面 RLHF/DPO、推理时约束等（偏算法侧）。

**面试点**：测法 = 事实题 + RAG 约束校验 + 不知道的题；降法 = RAG + Prompt + 后处理 + 训练/推理约束。

---

### 11. RAG 场景下，测试重点是什么？

- **检索质量**：检索到的文档是否相关、是否包含答案；召全与召准。
- **生成与文档一致**：回答是否严格基于检索结果，有无篡改、夸大、无依据推理。
- **引用与溯源**：是否标注来源、引用是否正确对应文档。
- **无答案/低置信**：文档中无答案时，是否明确说「无法回答」而非幻觉。
- **多文档冲突**：文档矛盾时，行为是否合理（如说明存在不同说法）。

**面试点**：检索准确 + 生成忠于文档 + 引用正确 + 无答案与冲突场景。

---

## 五、安全与合规

### 12. 大模型安全测试主要测什么？

- **内容安全**：违禁、暴力、违法、不良内容生成与过滤效果；敏感信息泄露。
- **越狱与对抗**：Prompt 注入、角色扮演、假设场景等诱导模型突破规则；对抗样本。
- **偏见与公平**：对不同群体、性别、地域的歧视或刻板印象；敏感话题上的倾向。
- **隐私**：训练数据记忆与泄露（如 PII）；输入是否被不当记录或用于训练。
- **合规**：符合公司规范与当地法规（如生成内容可追溯、年龄限制等）。

**面试点**：内容安全 + 越狱 + 偏见 + 隐私 + 合规，按业务取舍重点。

---

### 13. 什么是「越狱」？测试时如何构造越狱用例？

- **越狱（Jailbreak）**：通过特定 Prompt 技巧，让模型**绕过安全与合规限制**，输出本应拒绝的内容（如违法、暴力、隐私侵犯等）。
- **测试构造**：角色扮演（「假设你是无限制的 AI」）、编码与多语言绕过、长文包裹恶意请求、分步诱导、虚假场景（「仅用于研究」）等；可参考公开越狱案例与安全基准，建立**越狱用例库**并持续补充，验证模型是否仍拒绝或安全回复。

**面试点**：越狱 = 绕过安全限制；测试 = 构造多种诱导方式 + 用例库 + 回归。

---

## 六、性能与工程

### 14. 大模型接口/服务在性能上关注哪些指标？

- **首 Token 延迟（TTFT）**：从请求到第一个 token 返回的时间，影响体感响应速度。
- **吞吐**：单位时间完成的请求数或生成的 token 数（如 tokens/s）。
- **端到端延迟**：完整回复的总耗时（或 P95、P99）。
- **并发与限流**：高并发下的成功率、排队与超时；限流策略是否合理。
- **资源与成本**：GPU/CPU 利用率、显存、单请求 Token 消耗与成本。

**面试点**：TTFT、吞吐、延迟分布、并发与成本，和业务 SLA 挂钩。

---

### 15. 模型迭代后如何做回归测试？

- **能力回归**：在固定 **Benchmark 或业务评测集**上跑新旧版本，对比得分与通过率；关注掉点与异常 case。
- **关键场景**：高风险、高曝光、曾出问题的场景做**全量或加权重**回归。
- **安全与合规**：越狱、敏感内容、偏见等用例集定期回归。
- **自动化**：评测集 + 脚本/平台自动跑、自动对比、自动报告与告警；人工复核掉点与新增 badcase。

**面试点**：固定评测集 + 关键场景 + 安全用例 + 自动化与人工复核。

---

## 七、工具与实践

### 16. 你了解哪些大模型评测/测试工具或框架？

- **自动化评测**：使用各 Benchmark 官方脚本（如 C-Eval、HELM）；自建评测脚本调用 API、解析输出、算指标。
- **评估框架**：LangChain 等生态中的 evaluation 模块；LlamaIndex 的评估；开源评测框架（如 OpenCompass、lm-evaluation-harness 等）做多模型多任务评测。
- **人工评估**：内部标注平台、打分表、A/B 实验平台。
- **红队/安全**：Garak、专门越狱/安全评测工具与数据集。

**面试点**：能说出 1～2 个与岗位相关的工具或框架，并说明用来做什么（如回归、安全、人工标注）。

---

### 17. 如何构造高质量的测试/评测数据？

- **来源**：业务日志与真实用户 Query；产品与运营整理的典型与边界 case；从公开 Benchmark 筛选或改写；人工构造（领域专家 + 标注规范）。
- **设计**：覆盖**主流程、边界、对抗、多语言/多表述**；区分**能力维度**（理解、生成、推理、安全等）；标注**期望输出或评分标准**（便于自动或人工评估）；**持续补充** badcase 与线上问题。
- **质量**：标注规范、多人校验、抽样复核、去重与平衡（避免某类过多）。

**面试点**：真实 + 构造、覆盖主流程与边界、标注与规范、持续迭代。

---

### 18. 如何用大模型辅助测试（如生成用例、评估）？

- **用例生成**：用大模型根据需求/接口/场景描述生成测试用例、边界值、异常输入；需人工或规则校验与去重。
- **评估辅助**：用大模型对「模型输出」做初筛打分（如相关性、流畅度），再结合人工复核；或作为裁判模型做 A/B 偏好判断。注意**裁判模型自身有偏好与误差**，需校准与抽样校验。
- **总结与分类**：用大模型对 badcase 做归类、总结根因，辅助分析。

**面试点**：可辅助生成与评估，但要校验与复核，避免「模型测模型」的循环偏差。

---

## 八、场景与项目题

### 19. 从 0 搭建大模型质量保障体系，你会怎么做？

- **能力与范围**：明确产品形态（对话、RAG、代码、客服等）和**质量目标**（能力、安全、体验、性能）。
- **评测体系**：建立**业务评测集**（主流程 + 边界 + 安全）；接入或自建 **Benchmark** 做能力基线；定义**自动指标 + 人工评估**流程与周期。
- **流程**：需求/模型迭代时同步设计测试与验收标准；发布前**回归 + 关键场景 + 安全**门禁；发布后**线上监控与 badcase 回收**，反哺用例与数据。
- **工具与平台**：评测执行、结果对比、报告与看板；badcase 管理与分析；必要时红队与越狱用例库。
- **协作**：与算法、产品、合规对齐标准与优先级；建立 badcase 复盘与数据闭环。

**面试点**：目标清晰 + 评测集与基准 + 流程门禁 + 工具与数据闭环 + 协作。

---

### 20. 线上发现模型回答有事实错误或不当内容，你会如何排查与改进？

- **排查**：复现（相同/相似输入）→ 看**上下文、模型版本、参数**→ 判断是幻觉、检索错误、Prompt 问题还是越狱 → 查同类请求占比与影响面。
- **改进**：属幻觉则加强 RAG 约束或事实性校验、补充检索或知识；属 Prompt 则优化系统 Prompt 或增加约束；属安全则补充安全规则与越狱用例、模型侧加固；**将 case 纳入回归集与 badcase 库**，防止再发。
- **流程**：告警与反馈入口、分级处理、复盘与知识沉淀。

**面试点**：复现与归因 → 针对性改进（数据/Prompt/安全/模型）→ 用例与流程闭环。

---

### 21. 大模型测试中，你认为最大的挑战是什么？

- **可答方向**：**评估标准**难统一（多解、主观、长文本）；**非确定性**导致回归噪声与 flaky；**幻觉与安全**边界复杂、对抗持续演进；**成本与效率**（标注、算力、人工评估）；**与算法/产品对齐**「什么是好、什么是缺陷」。
- **建议**：选 1～2 点结合经历说，并简单说你的应对思路（如标准化流程、自动化+抽样人工、建设用例与数据闭环）。

**面试点**：体现对领域难点的理解，以及解决问题的思路。

---

## 九、小结速记

| 模块       | 关键词 |
|------------|--------|
| 基础       | 非确定性、多维评估、人工+自动、幻觉概念与原因 |
| Prompt     | Prompt 鲁棒性、注入、越狱、多轮与边界         |
| 评估       | BLEU/ROUGE/准确率/语义、人工评估、Benchmark（C-Eval、HumanEval 等） |
| 幻觉与 RAG | 事实性测试、RAG 一致与引用、无答案与冲突      |
| 安全       | 内容安全、越狱、偏见、隐私、合规、用例库     |
| 工程       | TTFT、吞吐、回归评测集、自动化与报告         |
| 实践       | 数据构造、工具/框架、模型辅助测试、质量体系与线上闭环 |

---

*可与《Python自动化测试常见面试题》《测试开发常见面试题》等搭配复习，AI 大模型测试是当前测试岗位的高频方向。*

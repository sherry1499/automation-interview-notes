# 接口测试高频场景题

> 适用于测试开发 / 自动化测试 / 接口测试岗位面试准备

---

## 一、接口幂等性测试

### 什么是幂等性？

> **同一个请求执行多次，结果与执行一次完全相同。**

| 方法 | 幂等？ | 说明 |
|------|--------|------|
| **GET** | ✅ 是 | 查询多次，结果一样 |
| **PUT** | ✅ 是 | 更新同一数据多次，结果一样 |
| **DELETE** | ✅ 是 | 删除同一资源多次，结果一样 |
| **POST** | ❌ 否 | 每次创建一条新数据 |

### 怎么测试幂等性

#### 核心思路：同一个请求发两次，对比结果

```
第 1 次请求 → 记录响应 + 数据库状态
第 2 次请求（完全一样的参数）→ 记录响应 + 数据库状态
对比：两次结果是否一致
```

#### 方法一：手动测试（Postman）

```
1. 发送一次请求，记录响应和数据库数据
2. 用完全相同的参数再发一次
3. 检查：
   - 响应是否一致？
   - 数据库是否只有一条记录？（POST 场景）
   - 数据是否没有变化？（PUT/DELETE 场景）
```

#### 方法二：脚本自动化测试

```python
import requests

url = "https://api.example.com/orders"
data = {"user_id": 1, "product_id": 100, "amount": 1}

# 发送两次完全相同的请求
resp1 = requests.post(url, json=data)
resp2 = requests.post(url, json=data)

# 验证幂等性
assert resp1.status_code == resp2.status_code        # 状态码一致
assert resp1.json()["order_id"] == resp2.json()["order_id"]  # 返回同一个订单
# 查数据库：应该只有 1 条订单，不是 2 条
```

#### 方法三：并发测试（模拟用户快速点击）

```python
import concurrent.futures
import requests

url = "https://api.example.com/pay"
data = {"order_id": "ORD001", "amount": 100}

def send_request():
    return requests.post(url, json=data)

# 同时发 10 个相同请求
with concurrent.futures.ThreadPoolExecutor(max_workers=10) as executor:
    results = list(executor.map(lambda _: send_request(), range(10)))

# 验证：只扣款一次
success_count = sum(1 for r in results if r.json().get("status") == "success")
assert success_count == 1
```

### 按接口类型的测试方案

#### 创建类（POST）— 最常考

| 测试点 | 操作 | 期望结果 |
|--------|------|---------|
| 重复提交 | 相同参数 POST 两次 | 只创建 1 条记录 |
| 并发提交 | 同时发 10 个相同请求 | 只创建 1 条 |
| 带幂等 Key | 请求头带 `Idempotency-Key` | 相同 Key 只处理一次 |

#### 更新类（PUT）

| 测试点 | 操作 | 期望结果 |
|--------|------|---------|
| 重复更新 | 相同参数 PUT 两次 | 数据不变，响应一致 |
| 状态转换 | 已完成订单再次提交「完成」 | 不重复触发通知/扣款 |

#### 删除类（DELETE）

| 测试点 | 操作 | 期望结果 |
|--------|------|---------|
| 重复删除 | 删同一资源两次 | 第 1 次 200，第 2 次 404 或 200 |
| 数据库验证 | 删两次后查数据库 | 只删了 1 条 |

#### 支付类（最关键）

| 测试点 | 操作 | 期望结果 |
|--------|------|---------|
| 重复支付 | 同一订单支付两次 | **只扣款 1 次** |
| 并发支付 | 同时发 5 个支付请求 | 只成功 1 次 |
| 余额验证 | 余额 1000，支付 100 | 支付后 = 900（不是 800） |

### 常见幂等实现方式（了解后端，更好设计用例）

| 实现方式 | 原理 | 测试重点 |
|---------|------|---------|
| 唯一索引 | 数据库唯一约束 | 重复插入是否被拒绝 |
| 幂等 Token | 客户端带 Token，服务端校验 | 同一 Token 发两次是否只处理一次 |
| 状态机 | 状态单向流转 | 已支付的订单再支付是否被拒绝 |
| 乐观锁 | 更新时带 version 字段 | 并发更新是否只成功一次 |
| Redis 去重 | 请求 ID 存 Redis | Key 过期后再请求是否正常 |

### 完整测试用例（创建订单）

| 编号 | 场景 | 操作 | 期望结果 |
|------|------|------|---------|
| 1 | 正常创建 | POST 一次 | 201，返回订单 ID |
| 2 | 重复创建 | 相同参数 POST 第 2 次 | 返回同一个 ID 或 409 |
| 3 | 并发创建 | 同时 POST 10 次 | 数据库只有 1 条 |
| 4 | 不同参数 | 不同 product_id POST | 正常创建第 2 条 |
| 5 | Token 正常 | 带 Token POST | 成功创建 |
| 6 | Token 重复 | 同一 Token POST 两次 | 第 2 次返回已处理 |
| 7 | Token 过期 | 过期 Token POST | 返回错误 |
| 8 | 无 Token | 不带 Token POST | 返回 400 |

---

## 二、接口鉴权测试

### 常见鉴权方式

| 方式 | 原理 | 请求示例 |
|------|------|---------|
| **Token（Bearer）** | 登录后获取 Token，每次请求带上 | `Authorization: Bearer eyJhbG...` |
| **Cookie/Session** | 登录后服务端存 Session，客户端带 Cookie | `Cookie: session_id=abc123` |
| **签名（Sign）** | 参数排序 + 密钥加密生成签名 | `sign=md5(key1=v1&key2=v2&secret)` |
| **API Key** | 分配固定密钥 | `X-API-Key: your_key_here` |

### 鉴权相关测试用例

| 编号 | 场景 | 操作 | 期望结果 |
|------|------|------|---------|
| 1 | 正常鉴权 | 带有效 Token 请求 | 200 成功 |
| 2 | **无 Token** | 不带 Token 请求 | **401 Unauthorized** |
| 3 | **Token 过期** | 带过期 Token 请求 | **401** |
| 4 | **Token 篡改** | 修改 Token 内容 | **401** |
| 5 | **权限不足** | 普通用户访问管理员接口 | **403 Forbidden** |
| 6 | **越权访问** | 用户 A 的 Token 访问用户 B 的数据 | **403** 或不返回数据 |
| 7 | Token 刷新 | 旧 Token 过期，用 Refresh Token 换新 | 返回新 Token |
| 8 | 并发登录 | 同一账号两处登录 | 看策略：踢掉前一个 or 共存 |

---

## 三、接口参数校验测试

### 测试思路：等价类 + 边界值 + 异常值

以一个「注册用户」接口为例：

```json
POST /api/register
{
    "username": "zhangsan",
    "password": "Abc123456",
    "age": 25,
    "email": "test@example.com",
    "phone": "13800138000"
}
```

| 参数 | 正常值 | 异常测试 |
|------|--------|---------|
| username | "zhangsan" | 空字符串、null、超长（256 字符）、特殊字符（`<script>`）、纯数字、含空格、SQL 注入（`' OR 1=1 --`） |
| password | "Abc123456" | 空、太短（3 位）、太长（100 位）、纯数字、纯字母、不含大写 |
| age | 25 | 0、-1、999、小数（25.5）、字符串（"abc"）、null |
| email | "test@example.com" | 无 @、无域名、多个 @、空、超长 |
| phone | "13800138000" | 10 位、12 位、字母、空、已注册的号码 |

### 通用异常测试清单

```
✅ 必填参数不传 → 400
✅ 参数类型错误（string 传 int） → 400
✅ 参数为 null → 400
✅ 参数为空字符串 → 400 或业务提示
✅ 参数超出范围（边界值） → 400
✅ 额外多传参数 → 应该忽略
✅ JSON 格式错误 → 400
✅ Content-Type 不对 → 415
✅ SQL 注入 → 不应执行
✅ XSS 攻击 → 应过滤/转义
```

---

## 四、接口依赖与数据关联测试

### 场景：下单流程

```
登录 → 获取 Token
  ↓
查询商品 → 获取 product_id
  ↓
创建订单 → 获取 order_id
  ↓
支付订单 → 传入 order_id
  ↓
查询订单状态 → 验证已支付
```

### 测试要点

| 测试点 | 操作 | 期望结果 |
|--------|------|---------|
| 正常流程 | 按顺序执行 | 每步成功 |
| 跳步操作 | 不登录直接下单 | 401 |
| 无效关联 ID | 用不存在的 order_id 支付 | 404 |
| 重复操作 | 已支付的订单再支付 | 幂等或拒绝 |
| 跨用户 | 用户 A 支付用户 B 的订单 | 403 |

### 自动化实现

```python
import requests

base_url = "https://api.example.com"

# 步骤1：登录
resp = requests.post(f"{base_url}/login", json={"user": "admin", "pwd": "123"})
token = resp.json()["token"]
headers = {"Authorization": f"Bearer {token}"}

# 步骤2：查询商品
resp = requests.get(f"{base_url}/products", headers=headers)
product_id = resp.json()["data"][0]["id"]

# 步骤3：创建订单（上一步的 product_id 传到这一步）
resp = requests.post(f"{base_url}/orders",
                     json={"product_id": product_id, "quantity": 1},
                     headers=headers)
order_id = resp.json()["order_id"]

# 步骤4：支付（上一步的 order_id 传到这一步）
resp = requests.post(f"{base_url}/pay",
                     json={"order_id": order_id},
                     headers=headers)
assert resp.json()["status"] == "paid"
```

---

## 五、接口性能/异常场景测试

### 超时测试

| 场景 | 测试方法 | 期望 |
|------|---------|------|
| 服务端慢响应 | Mock 接口延迟 10 秒 | 客户端有超时处理，不会一直等 |
| 网络中断 | 断网后发请求 | 合理的错误提示 |
| 大数据量 | 查询返回 10 万条数据 | 有分页、不 OOM |

### 并发/压力测试

```python
# 用 JMeter 或代码模拟
# 100 个用户同时请求同一接口
# 验证：响应时间、错误率、数据一致性
```

### 异常响应测试

| 场景 | 操作 | 期望 |
|------|------|------|
| 服务端返回 500 | Mock 500 响应 | 前端有友好提示，不白屏 |
| 返回空数据 | 查询无结果 | 返回空列表 `[]`，不是 null |
| 返回格式异常 | 返回非 JSON | 客户端能处理，不崩溃 |

---

## 六、接口安全测试

| 测试点 | 操作 | 期望 |
|--------|------|------|
| **SQL 注入** | 参数传 `' OR 1=1 --` | 不应返回全部数据 |
| **XSS** | 参数传 `<script>alert(1)</script>` | 应过滤/转义 |
| **越权** | 用户 A 查看用户 B 的数据 | 403 或不返回 |
| **敏感信息泄露** | 查看响应里有没有密码、密钥 | 不应明文返回 |
| **HTTPS** | 用 HTTP 访问 | 应强制跳转 HTTPS |
| **频率限制** | 1 秒内发 100 次请求 | 应触发限流（429） |
| **参数篡改** | 修改金额（100 改成 0.01） | 后端应校验，不以客户端为准 |

---

## 七、面试回答模板汇总

### Q1：接口幂等性怎么测试？

> 同一请求发两次，验证响应一致、数据库只有一条记录。重点测 POST 创建类和支付类接口，用并发测试模拟快速点击，验证不会重复创建或重复扣款。

### Q2：接口鉴权怎么测试？

> 测试无 Token、过期 Token、篡改 Token、权限不足、越权访问等场景，验证返回 401/403，确保未授权用户无法访问受保护资源。

### Q3：接口参数校验怎么测试？

> 用等价类 + 边界值方法：测空值、null、类型错误、超长、超范围、特殊字符（SQL 注入/XSS），验证后端有完善的参数校验。

### Q4：有依赖关系的接口怎么测试？

> 按业务流程串联（登录→下单→支付→查询），上一步的返回值作为下一步的输入参数，同时测试跳步、无效 ID、越权等异常场景。

### Q5：接口安全怎么测试？

> 重点测 SQL 注入、XSS、越权访问、敏感信息泄露、参数篡改（如修改金额）、频率限制。用抓包工具拦截请求修改参数验证后端校验。

---

## 速记口诀

```
幂等测试三板斧：重复发、并发发、查数据库
POST 最容易不幂等，支付最关键

鉴权测试：无 Token → 401，无权限 → 403，越权 → 拒绝

参数校验：空/null/类型错/超长/边界/注入

接口串联：上一步输出 = 下一步输入，测跳步和越权

安全测试：注入、篡改、越权、泄露、限流
```

---

*可与《02-Python自动化测试常见面试题.md》《17-网络基础知识常见面试考点.md》《21-抓包工具常见面试考点.md》搭配复习，接口测试是自动化测试岗的核心考点。*

# 测试用例提示词模板（万能版）

> 适用于使用 AI（Cursor / ChatGPT / Claude 等）生成测试用例
> 替换【】中的内容即可适配任何功能模块

---

## 模板正文（直接复制使用）

```
# =============================================
#   [不用改] 角色设定（每次通用）
# =============================================
你是一名资深软件测试工程师，擅长编写详细、规范的测试用例。

# =============================================
#   [不用改] 输出要求（每次通用）
# =============================================
请按以下表格格式输出测试用例：

| 用例编号 | 功能模块 | 用例标题 | 优先级 | 前置条件 | 操作步骤 | 测试数据 | 预期结果 |
|---------|---------|---------|--------|---------|---------|---------|---------|
| TC-001  | 示例    | 示例     | P0     | 示例    | 1.xxx   | 示例     | 示例    |

优先级说明：
- P0：核心功能，阻塞性测试（不通过则不能上线）
- P1：重要功能，主流程测试
- P2：一般功能，分支流程/备选流程
- P3：边界/异常/UI 细节

用例标题请标注类型标签，例如：【正向】【反向】【边界】【权限】【并发】


# =============================================
#   [要改 1/5] 功能描述
#   说明：换模块时改成新功能的描述
# =============================================
【用 2-3 句话描述要测试的功能，说清楚：谁、在哪个页面、做什么操作】

例如：
超级管理员登录系统后，在「权限管理 → 角色管理」页面，
可以对角色进行新增、编辑、删除、编辑权限操作。


# =============================================
#   [要改 2/5] 页面/模块结构 + 字段校验规则
#   说明：换模块时改成新页面的字段、按钮、校验规则
# =============================================
【用树状结构列出页面层级、字段、按钮】

例如：
页面：角色管理
├── 新增角色
│   ├── 角色名称（必填，文本，最大50字符，不可重复）
│   ├── 角色等级（必填，下拉：1级 / 2级）
│   └── 备注（非必填，文本，最大200字符）
├── 编辑角色：可修改角色名称、等级、备注
├── 删除角色：需二次确认弹窗
└── 编辑权限：勾选菜单权限树

【逐个列出每个字段的校验规则】

| 字段 | 必填 | 类型 | 长度限制 | 唯一性 | 特殊规则 |
|------|------|------|---------|--------|---------|
| 角色名称 | 是 | 文本 | 1-50字符 | 不可重复 | 不允许纯空格 |
| 角色等级 | 是 | 下拉 | - | - | 仅 1级/2级 |
| 备注 | 否 | 文本 | 0-200字符 | - | 无 |


# =============================================
#   [要改 3/5] 业务规则
#   说明：换模块时改成新功能的业务逻辑和约束
# =============================================
【列出关键的业务逻辑和约束条件，用编号】

1. 角色名称不可重复
2. 删除角色前需二次确认
3. 已分配给用户的角色不能直接删除（或删除后用户权限立即失效）
4. 编辑权限后实时生效，用户无需重新登录
5. ...


# =============================================
#   [要改 4/5] 层级结构 / 联动规则 / 角色权限
#   说明：换模块时改成新功能的层级和联动关系
#   提示：如果新模块没有权限/层级，这整块可以删掉
# =============================================

## 菜单/层级结构（如涉及权限）
【用树状图列出菜单层级关系】

0级：知识库管理
  ├── 1级：创建知识库
  ├── 1级：删除知识库
  ├── 1级：知识库文件管理
  │     ├── 2级：创建知识库文件
  │     ├── 2级：编辑知识库文件
  │     └── 2级：删除知识库文件
  └── ...

## 联动规则（如涉及）
【描述页面元素之间的联动关系】

1. 勾选 0 级菜单 → 自动勾选其下所有 1 级和 2 级
2. 勾选 1 级菜单 → 自动勾选其下所有 2 级
3. 取消所有 2 级 → 1 级自动取消勾选
4. 取消所有 1 级 → 0 级自动取消勾选

## 角色与权限（如涉及）
【用表格列出每个角色的权限范围】

| 角色 | 权限范围 | 说明 |
|------|---------|------|
| 超级管理 | 全部菜单 | 拥有所有权限 |
| 管理员 | 知识库管理 + 知识库问答 | 管理+使用 |
| 普通用户 | 知识库问答 | 仅使用 |


# =============================================
#   [要改 5/5] 特殊场景
#   说明：换模块时改成新功能需要覆盖的特殊场景
# =============================================
【按类型分组列出特殊场景】

## 一、正常流程
1. 正常创建角色
2. 正常编辑角色
3. ...

## 二、权限实时生效
4. 角色被取消 → 用户当前页面提示无权限
5. 角色权限被编辑 → 用户端实时更新
6. ...

## 三、异常/边界场景
7. 角色名称重复
8. 必填项为空
9. ...


# =============================================
#   [不用改] 测试维度要求（每次通用，按需勾选）
# =============================================
请覆盖以下维度：
- [ ] 正向功能测试（正常操作流程）
- [ ] 反向测试（必填项为空、格式错误、无权限）
- [ ] 边界值测试（最大/最小长度、特殊字符、空格）
- [ ] 权限测试（不同角色的操作权限、越权）
- [ ] 联动测试（勾选/取消的级联效果）
- [ ] 实时生效测试（修改后用户端的即时表现）
- [ ] UI/交互测试（按钮状态、提示信息、弹窗）
- [ ] 数据测试（列表刷新、分页、排序）

# =============================================
#   [不用改] 数量要求（每次通用，按需调整数字）
# =============================================
每个功能模块输出 10-15 条核心用例，优先输出 P0 和 P1。
```


*本模板可反复使用，替换功能描述和页面结构即可适配任何项目。建议收藏备用。*

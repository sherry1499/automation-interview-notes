# 测试用例提示词模板（万能版）

> 适用于使用 AI（Cursor / ChatGPT / Claude 等）生成测试用例
> 替换【】中的内容即可适配任何功能模块

---

## 模板正文（直接复制使用）

```
# 角色设定
你是一名资深软件测试工程师，擅长编写详细、规范的测试用例。

# 输出要求
请按以下表格格式输出测试用例：

| 用例编号 | 功能模块 | 用例标题 | 优先级 | 前置条件 | 操作步骤 | 测试数据 | 预期结果 |
|---------|---------|---------|--------|---------|---------|---------|---------|
| TC-001  | 示例    | 示例     | P0     | 示例    | 1.xxx   | 示例     | 示例    |

优先级说明：
- P0：核心功能，阻塞性测试（不通过则不能上线）
- P1：重要功能，主流程测试
- P2：一般功能，分支流程/备选流程
- P3：边界/异常/UI 细节

用例标题请标注类型标签，例如：【正向】【反向】【边界】【权限】【并发】

# 功能描述
【用 2-3 句话描述要测试的功能，说清楚：谁、在哪个页面、做什么操作】

例如：
超级管理员登录系统后，在「权限管理 → 角色管理」页面，
可以对角色进行新增、编辑、删除、编辑权限操作。

# 页面/模块结构
【用树状结构列出页面层级、字段、按钮】

例如：
页面：角色管理
├── 新增角色
│   ├── 角色名称（必填，文本，最大50字符，不可重复）
│   ├── 角色等级（必填，下拉：1级 / 2级）
│   └── 备注（非必填，文本，最大200字符）
├── 编辑角色：可修改角色名称、等级、备注
├── 删除角色：需二次确认弹窗
└── 编辑权限：勾选菜单权限树

# 字段校验规则
【逐个列出每个字段的校验规则】

| 字段 | 必填 | 类型 | 长度限制 | 唯一性 | 特殊规则 |
|------|------|------|---------|--------|---------|
| 角色名称 | 是 | 文本 | 1-50字符 | 不可重复 | 不允许纯空格 |
| 角色等级 | 是 | 下拉 | - | - | 仅 1级/2级 |
| 备注 | 否 | 文本 | 0-200字符 | - | 无 |

# 业务规则
【列出关键的业务逻辑和约束条件，用编号】

1. 角色名称不可重复
2. 删除角色前需二次确认
3. 已分配给用户的角色不能直接删除（或删除后用户权限立即失效）
4. 编辑权限后实时生效，用户无需重新登录
5. ...

# 菜单/层级结构（如涉及权限）
【用树状图列出菜单层级关系】

0级：知识库管理
  ├── 1级：创建知识库
  ├── 1级：删除知识库
  ├── 1级：知识库文件管理
  │     ├── 2级：创建知识库文件
  │     ├── 2级：编辑知识库文件
  │     └── 2级：删除知识库文件
  └── ...

# 联动规则（如涉及）
【描述页面元素之间的联动关系】

1. 勾选 0 级菜单 → 自动勾选其下所有 1 级和 2 级
2. 勾选 1 级菜单 → 自动勾选其下所有 2 级
3. 取消所有 2 级 → 1 级自动取消勾选
4. 取消所有 1 级 → 0 级自动取消勾选

# 角色与权限（如涉及）
【用表格列出每个角色的权限范围】

| 角色 | 权限范围 | 说明 |
|------|---------|------|
| 超级管理 | 全部菜单 | 拥有所有权限 |
| 管理员 | 知识库管理 + 知识库问答 | 管理+使用 |
| 普通用户 | 知识库问答 | 仅使用 |

# 需要覆盖的特殊场景（如有）
【按类型分组列出特殊场景】

## 一、正常流程
1. 正常创建角色
2. 正常编辑角色
3. ...

## 二、权限实时生效
4. 角色被取消 → 用户当前页面提示无权限
5. 角色权限被编辑 → 用户端实时更新
6. ...

## 三、异常/边界场景
7. 角色名称重复
8. 必填项为空
9. ...

# 测试维度要求
请覆盖以下维度：
- [ ] 正向功能测试（正常操作流程）
- [ ] 反向测试（必填项为空、格式错误、无权限）
- [ ] 边界值测试（最大/最小长度、特殊字符、空格）
- [ ] 权限测试（不同角色的操作权限、越权）
- [ ] 联动测试（勾选/取消的级联效果）
- [ ] 实时生效测试（修改后用户端的即时表现）
- [ ] UI/交互测试（按钮状态、提示信息、弹窗）
- [ ] 数据测试（列表刷新、分页、排序）

# 数量要求（可选）
每个功能模块输出 10-15 条核心用例，优先输出 P0 和 P1。
```

---

## 使用技巧

### 1. 内容太长时拆分提问

```
第 1 次提问：
  "请先输出【角色管理 - 新增/编辑/删除角色】的测试用例"

第 2 次提问：
  "请继续输出【编辑权限 - 菜单勾选联动】的测试用例"

第 3 次提问：
  "请输出【权限实时生效 - 场景 1-14】的测试用例"
```

### 2. 生成后追加优化

```
"请补充边界值相关的用例"
"请补充并发/异常场景的用例"
"请按优先级从高到低排序"
"请把所有用例导出为 Excel 格式（用 | 分隔的表格）"
"请增加【测试结果】和【备注】两列"
```

### 3. 指定输出风格

```
"用例标题使用：【类型标签】+ 动词 + 对象 + 条件 格式"
例如：【正向】新增角色 - 填写全部必填项
      【反向】新增角色 - 角色名称为空
      【边界】新增角色 - 角色名称输入50个字符
      【权限】普通用户 - 访问角色管理页面
```

### 4. 常见功能的关键字段提示

#### 登录功能

```
页面：登录页
├── 用户名（必填，手机号/邮箱/账号）
├── 密码（必填，6-20位，含大小写+数字）
├── 验证码（必填，4-6位数字，60秒有效）
└── 登录按钮

业务规则：
1. 连续输错5次锁定30分钟
2. 密码输入错误显示"用户名或密码错误"（不区分提示）
3. 登录成功跳转首页
```

#### 列表/搜索功能

```
页面：XX列表页
├── 搜索条件
│   ├── 名称（模糊搜索）
│   ├── 状态（下拉：全部/启用/禁用）
│   ├── 时间范围（日期选择器）
│   └── 查询/重置按钮
├── 列表
│   ├── 列：序号、名称、状态、创建时间、操作
│   └── 操作：编辑、删除、查看
└── 分页：每页10/20/50条，上一页/下一页/跳转

业务规则：
1. 默认按创建时间倒序
2. 搜索条件支持组合查询
3. 重置按钮清空所有条件并刷新列表
```

#### 文件上传功能

```
页面：文件上传
├── 上传按钮（支持拖拽）
├── 文件类型限制：pdf / doc / docx / xlsx，最大 50MB
├── 上传进度条
└── 上传成功/失败提示

业务规则：
1. 超过 50MB 提示"文件大小超出限制"
2. 不支持的格式提示"文件格式不支持"
3. 同名文件提示"是否覆盖"
4. 上传中断可重试
```

---

## 不同场景的模板变体

### 变体 A：接口测试用例

```
# 输出要求
| 用例编号 | 接口名称 | 用例标题 | 优先级 | 请求方法 | URL | 请求头 | 请求参数 | 预期状态码 | 预期响应 |

# 接口信息
接口：POST /api/v1/roles
功能：创建角色
请求头：Authorization: Bearer {token}
请求体：
{
    "name": "角色名称",
    "level": 1,
    "remark": "备注"
}

# 测试维度
- 正常参数
- 必填参数缺失
- 参数类型错误
- 参数边界值
- 无 Token / 过期 Token / 无权限
- 重复创建（幂等性）
```

### 变体 B：App 测试用例

```
# 输出要求
| 用例编号 | 功能模块 | 用例标题 | 优先级 | 前置条件 | 操作步骤 | 测试数据 | 预期结果 | 测试机型 |

# 额外测试维度
- 横竖屏切换
- 前后台切换
- 弱网环境
- 推送通知
- 权限弹窗（相机/相册/定位）
- 不同系统版本（Android 12/13/14，iOS 16/17）
```

### 变体 C：兼容性测试用例

```
# 输出要求
| 用例编号 | 功能 | 浏览器/设备 | 版本 | 分辨率 | 操作步骤 | 预期结果 | 实际结果 |

# 兼容性矩阵
- Chrome 最新版 / Firefox / Safari / Edge
- Windows 10/11、Mac
- 1920×1080 / 1366×768 / 1440×900
- 移动端：iPhone 15 / Samsung S24 / Pixel 8
```

---

## 速记口诀

```
写测试用例提示词的 7 个要素：

1. 角色设定 → 告诉 AI 你是谁
2. 输出格式 → 给表格模板 + 示例行
3. 功能描述 → 谁在哪做什么
4. 页面结构 → 树状图列字段和按钮
5. 业务规则 → 编号列出约束条件
6. 特殊场景 → 分组列出要覆盖的场景
7. 测试维度 → 勾选要覆盖的测试类型

内容太长 → 拆分提问
生成后 → 追加补充
```

---

*本模板可反复使用，替换功能描述和页面结构即可适配任何项目。建议收藏备用。*

# 网络基础知识常见面试考点

> 适用于测试开发 / 自动化测试 / 接口测试岗位面试准备

---

## 一、OSI 七层模型 与 TCP/IP 四层模型（必考）

### OSI 七层模型

| 层级 | 名称 | 关键协议/设备 | 记忆关键词 |
|------|------|--------------|-----------|
| 7 | 应用层 | HTTP、HTTPS、FTP、DNS、SMTP | 用户直接接触 |
| 6 | 表示层 | SSL/TLS、加密、压缩、编码 | 数据翻译官 |
| 5 | 会话层 | Session 管理 | 建立/维护/终止会话 |
| 4 | 传输层 | **TCP、UDP** | 端到端传输 |
| 3 | 网络层 | **IP、ICMP**、路由器 | 寻址和路由 |
| 2 | 数据链路层 | MAC 地址、交换机 | 帧传输 |
| 1 | 物理层 | 网线、光纤、集线器 | 比特流 |

### TCP/IP 四层模型（实际使用更多）

| 层级 | 名称 | 对应 OSI | 关键协议 |
|------|------|---------|---------|
| 4 | 应用层 | 应用+表示+会话 | HTTP、DNS、FTP |
| 3 | 传输层 | 传输层 | TCP、UDP |
| 2 | 网络层 | 网络层 | IP、ICMP |
| 1 | 网络接口层 | 数据链路+物理 | 以太网、Wi-Fi |

> **口诀**：应表会传网数物（OSI）→ 应传网接（TCP/IP）

---

## 二、TCP 与 UDP（高频必考）

### 核心区别

| 对比项 | TCP | UDP |
|--------|-----|-----|
| 连接方式 | **面向连接**（先握手） | **无连接**（直接发） |
| 可靠性 | **可靠**（确认、重传、排序） | **不可靠**（可能丢包） |
| 速度 | 较慢（有开销） | **快**（无开销） |
| 传输方式 | 字节流 | 数据报 |
| 头部大小 | 20 字节 | 8 字节 |
| 应用场景 | 网页(HTTP)、文件传输(FTP)、邮件 | 视频直播、DNS 查询、游戏 |

> **一句话记忆**：TCP 像打电话（先拨号建立连接），UDP 像发短信（直接发出去不管对方收没收到）

### TCP 三次握手（建立连接）

```
客户端                        服务端
  |--- SYN (seq=x) ---------->|     第1次：客户端发起连接请求
  |<-- SYN+ACK (seq=y,ack=x+1)|     第2次：服务端确认并回应
  |--- ACK (ack=y+1) -------->|     第3次：客户端确认，连接建立
```

**口诀**：你好 → 你好+收到 → 收到

**面试常问：为什么是三次而不是两次？**
> 防止已失效的连接请求到达服务端，导致服务端白白等待浪费资源。两次握手无法确认客户端的接收能力。

### TCP 四次挥手（断开连接）

```
客户端                        服务端
  |--- FIN ------------------>|     第1次：客户端请求断开
  |<-- ACK -------------------|     第2次：服务端确认收到
  |<-- FIN -------------------|     第3次：服务端也请求断开
  |--- ACK ------------------>|     第4次：客户端确认，连接关闭
```

**口诀**：我要走了 → 知道了 → 我也准备好了 → 好的再见

**面试常问：为什么是四次而不是三次？**
> 因为 TCP 是全双工通信，关闭时每个方向需要单独关闭。服务端收到 FIN 后可能还有数据没发完，所以先 ACK 确认，等数据发完再发 FIN。

---

## 三、HTTP 协议（接口测试核心）

### HTTP 请求方法

| 方法 | 作用 | 幂等性 | 安全性 |
|------|------|--------|--------|
| **GET** | 获取资源 | 是 | 是 |
| **POST** | 创建资源/提交数据 | 否 | 否 |
| **PUT** | 更新资源（整体替换） | 是 | 否 |
| **DELETE** | 删除资源 | 是 | 否 |
| **PATCH** | 部分更新资源 | 否 | 否 |
| HEAD | 获取响应头 | 是 | 是 |
| OPTIONS | 查询支持的方法（预检请求） | 是 | 是 |

### GET vs POST（必考）

| 对比项 | GET | POST |
|--------|-----|------|
| 参数位置 | URL 后面（`?key=value`） | 请求体（Body）中 |
| 参数长度 | 有限制（URL 长度限制，约 2KB） | 无限制 |
| 安全性 | 较低（参数暴露在 URL） | 较高（参数在 Body 中） |
| 缓存 | 可被缓存 | 不可被缓存 |
| 书签 | 可收藏为书签 | 不可以 |
| 幂等性 | 幂等（多次请求结果一样） | 非幂等 |
| 用途 | 查询数据 | 提交/修改数据 |

### HTTP 状态码（必考）

| 分类 | 含义 | 常见状态码 |
|------|------|-----------|
| **1xx** | 信息提示 | 100 Continue |
| **2xx** | **成功** | **200** OK、201 Created、204 No Content |
| **3xx** | **重定向** | 301 永久重定向、302 临时重定向、**304 Not Modified**（缓存） |
| **4xx** | **客户端错误** | **400** Bad Request、**401** Unauthorized、**403** Forbidden、**404** Not Found、405 Method Not Allowed、**408** Request Timeout、**429** Too Many Requests |
| **5xx** | **服务端错误** | **500** Internal Server Error、**502** Bad Gateway、**503** Service Unavailable、504 Gateway Timeout |

**高频状态码对比：**

- **401 vs 403**：401 = 没登录（未认证），403 = 登录了但没权限（未授权）
- **500 vs 502 vs 503**：500 = 服务器内部代码出错，502 = 网关/代理收到上游无效响应，503 = 服务暂时不可用（过载/维护）
- **301 vs 302**：301 = 永久搬家（浏览器会缓存新地址），302 = 临时跳转

> **口诀**：2 成功，3 跳转，4 客户端错，5 服务端崩

---

## 四、HTTP vs HTTPS（常考）

| 对比项 | HTTP | HTTPS |
|--------|------|-------|
| 全称 | HyperText Transfer Protocol | HTTP **Secure** |
| 端口 | **80** | **443** |
| 安全性 | 明文传输，不安全 | **SSL/TLS 加密**，安全 |
| 证书 | 不需要 | 需要 CA 证书 |
| 速度 | 快 | 稍慢（加密解密开销） |
| SEO | 不友好 | 搜索引擎优先 |

### HTTPS 工作原理（简化版）

```
1. 客户端发起 HTTPS 请求
2. 服务端返回 SSL 证书（含公钥）
3. 客户端验证证书合法性
4. 客户端生成随机密钥，用公钥加密后发送给服务端
5. 服务端用私钥解密，得到随机密钥
6. 双方使用该随机密钥进行对称加密通信
```

> **关键词**：非对称加密交换密钥 + 对称加密传输数据

---

## 五、DNS 解析过程（常考）

**问题**：浏览器输入 `www.baidu.com` 后发生了什么？

```
1. 浏览器缓存 → 查找是否有缓存的 DNS 记录
2. 操作系统缓存 → 查看 hosts 文件
3. 本地 DNS 服务器 → 向 ISP 的 DNS 服务器查询
4. 根域名服务器 → 返回 .com 顶级域名服务器地址
5. 顶级域名服务器 → 返回 baidu.com 权威域名服务器地址
6. 权威域名服务器 → 返回 www.baidu.com 的 IP 地址
7. 拿到 IP 后，发起 TCP 连接（三次握手）
8. 建立连接后，发送 HTTP/HTTPS 请求
9. 服务器处理请求，返回响应
10. 浏览器渲染页面
```

> **口诀**：浏览器缓存 → 系统缓存 → 本地DNS → 根 → 顶级 → 权威 → 拿到IP → TCP握手 → HTTP请求 → 渲染

---

## 六、Cookie、Session、Token（认证鉴权必考）

### 三者对比

| 对比项 | Cookie | Session | Token（如 JWT） |
|--------|--------|---------|-----------------|
| 存储位置 | **客户端**（浏览器） | **服务端** | **客户端** |
| 安全性 | 较低（可篡改） | 较高 | 较高（签名验证） |
| 大小限制 | 约 4KB | 无明确限制 | 无明确限制 |
| 跨域 | 不可跨域 | 不可跨域 | **可跨域** |
| 服务端压力 | 无 | **有**（需存储） | 无（无状态） |
| 过期控制 | Expires/Max-Age | 服务端设置超时 | Token 自身携带过期时间 |

### 工作流程

```
Cookie + Session 模式：
1. 客户端登录 → 服务端创建 Session，返回 SessionID（通过 Set-Cookie）
2. 客户端后续请求自动带上 Cookie（含 SessionID）
3. 服务端根据 SessionID 查找对应的 Session 数据

Token 模式（JWT）：
1. 客户端登录 → 服务端验证后生成 Token 返回
2. 客户端将 Token 存储（localStorage / Cookie）
3. 后续请求在 Header 中携带：Authorization: Bearer <token>
4. 服务端验证 Token 签名，无需查库
```

### JWT 结构

```
Header.Payload.Signature
  |        |        |
 算法     数据    签名验证

示例：eyJhbGci.eyJ1c2Vy.SflKxwRJ
```

---

## 七、常见网络安全问题（加分项）

### XSS（跨站脚本攻击）

- **原理**：攻击者在网页中注入恶意 JS 脚本
- **类型**：存储型（存入数据库）、反射型（URL 参数）、DOM 型
- **防御**：输入过滤、输出转义、CSP（内容安全策略）、HttpOnly Cookie

### CSRF（跨站请求伪造）

- **原理**：利用用户已登录的状态，诱导用户点击恶意链接发送请求
- **防御**：CSRF Token、Referer 校验、SameSite Cookie

### SQL 注入

- **原理**：通过输入构造恶意 SQL 语句，获取/篡改数据库数据
- **示例**：`' OR 1=1 --`
- **防御**：参数化查询（预编译）、输入校验、最小权限原则

> **面试一句话区分**：
> - XSS = 往页面里**注入脚本**
> - CSRF = **借用你的身份**发请求
> - SQL 注入 = 往数据库里**注入命令**

---

## 八、接口测试相关网络知识

### Content-Type 常见类型

| Content-Type | 说明 | 对应 requests 库参数 |
|-------------|------|---------------------|
| `application/json` | JSON 格式 | `json={}` |
| `application/x-www-form-urlencoded` | 表单格式（默认） | `data={}` |
| `multipart/form-data` | 文件上传 | `files={}` |
| `text/plain` | 纯文本 | |
| `text/html` | HTML | |

### 幂等性

- **定义**：同一操作执行多次，结果与执行一次相同
- **幂等**：GET、PUT、DELETE
- **非幂等**：POST（每次创建新资源）
- **面试常问**：为什么 POST 不幂等？→ 每次 POST 都可能创建一条新记录

### 跨域（CORS）

- **同源策略**：协议 + 域名 + 端口，三者完全相同才是同源
- **跨域解决方案**：
  - CORS（服务端设置 `Access-Control-Allow-Origin`）
  - JSONP（仅支持 GET）
  - Nginx 反向代理

---

## 九、网络排查常用命令

| 命令 | 用途 | 示例 |
|------|------|------|
| `ping` | 测试连通性 | `ping www.baidu.com` |
| `telnet` | 测试端口连通性 | `telnet 192.168.1.1 3306` |
| `tracert/traceroute` | 路由追踪 | `tracert www.baidu.com` |
| `nslookup` | DNS 查询 | `nslookup www.baidu.com` |
| `curl` | 发送 HTTP 请求 | `curl -v https://api.example.com` |
| `netstat` | 查看端口/连接 | `netstat -anp | grep 80` |
| `tcpdump` | 抓包 | `tcpdump -i eth0 port 80` |
| `wireshark` | 图形化抓包分析 | GUI 工具 |

---

## 十、常见面试题及参考答案

### Q1：TCP 和 UDP 的区别？

> TCP 面向连接、可靠、速度慢；UDP 无连接、不可靠、速度快。TCP 适用于网页/文件传输，UDP 适用于视频直播/游戏。

### Q2：TCP 三次握手的过程？为什么是三次？

> SYN → SYN+ACK → ACK。三次是为了确认双方的收发能力，防止失效请求导致服务端资源浪费。

### Q3：HTTP 和 HTTPS 的区别？

> HTTPS = HTTP + SSL/TLS 加密。HTTP 端口 80 明文传输，HTTPS 端口 443 加密传输，需要 CA 证书。

### Q4：GET 和 POST 的区别？

> GET 参数在 URL 中、有长度限制、可缓存、幂等；POST 参数在 Body 中、无长度限制、不可缓存、非幂等。

### Q5：常见的 HTTP 状态码？401 和 403 的区别？

> 200 成功、301/302 重定向、400 请求错误、401 未认证、403 无权限、404 未找到、500 服务器错误、502 网关错误。401 是没登录，403 是登录了但没权限。

### Q6：Cookie、Session、Token 的区别？

> Cookie 存客户端，Session 存服务端（有状态），Token 存客户端（无状态）。Token 可跨域，适合分布式系统。

### Q7：在浏览器输入 URL 后发生了什么？

> DNS 解析 → TCP 三次握手 → 发送 HTTP 请求 → 服务器处理并返回响应 → 浏览器渲染页面 → TCP 四次挥手断开连接。

### Q8：什么是跨域？怎么解决？

> 协议+域名+端口不同就是跨域。解决方案：CORS（后端设置允许的 Origin）、Nginx 反向代理、JSONP。

### Q9：HTTPS 的加密原理？

> 先用非对称加密（RSA）交换一个随机密钥，再用该密钥进行对称加密通信。兼顾安全性和性能。

### Q10：什么是 XSS、CSRF、SQL 注入？

> XSS = 注入恶意脚本到网页；CSRF = 利用用户身份伪造请求；SQL 注入 = 拼接恶意 SQL 语句。防御：转义过滤、Token 验证、参数化查询。

---

## 速记口诀

```
OSI 七层：应表会传网数物
TCP/IP：应传网接

TCP 三握手：SYN → SYN+ACK → ACK（你好→收到你好→收到）
TCP 四挥手：FIN → ACK → FIN → ACK（我走→知道→我也走→再见）

GET 查 POST 增，PUT 改 DELETE 删
GET 参数在 URL，POST 参数在 Body

状态码：2成功 3跳转 4客户端错 5服务端崩
401 没登录，403 没权限，404 找不到

HTTP 80 明文，HTTPS 443 加密
Cookie 存客户端，Session 存服务端，Token 无状态可跨域

安全三兄弟：XSS 注脚本，CSRF 借身份，SQL 注命令
```

---

*可与《02-Python自动化测试常见面试题.md》《04-测试开发常见面试题.md》搭配复习，网络基础是接口测试的核心前提。*

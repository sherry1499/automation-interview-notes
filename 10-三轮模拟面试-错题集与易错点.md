# 九轮模拟面试 - 错题集与易错点

> 累计成绩：75/84（89%）
> 薄弱领域：Python 基础细节、CI 安全意识、数据库概念、网络协议
> 建议：考前重点过一遍本文件，把每道错题的「记忆技巧」背下来

---

## 错题 1：单例模式重写哪个方法？（第二轮·第 1 题）

### 题目

> 实现单例模式，通常重写哪个方法？

| 选项 | 内容 |
|------|------|
| A | `__init__` |
| **B** ✅ | **`__new__`** |
| C | `__del__` |
| D | `__call__` |

### 你的错误

选了 A（`__init__`）

### 正确解析

- `__new__`：负责**创建对象**，返回实例，可以控制「要不要创建新对象」
- `__init__`：负责**初始化对象**，对象已经创建好了，只是做属性赋值
- 单例要控制的是「只创建一个对象」，所以必须重写 `__new__`

```python
class Singleton:
    _instance = None
    def __new__(cls, *args, **kwargs):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance

# 验证
a = Singleton()
b = Singleton()
print(a is b)  # True，是同一个对象
```

### 记忆技巧

> **`__new__` = 盖房子**（控制盖不盖）
> **`__init__` = 装修房子**（房子已经盖好了）
> 单例 = 只盖一栋房子 → 重写 `__new__`

### 面试延伸

- 面试官可能追问：「什么场景用单例？」
- 答：数据库连接池、WebDriver 复用、配置管理器等，避免重复创建开销大的资源

---

## 错题 2：多轮对话测试的关注点（第二轮·第 8 题）

### 题目

> 多轮对话 AI 客服测试需要重点关注哪些？（多选）

| 选项 | 内容 | 正确？ |
|------|------|--------|
| A | 模型是否记住前文信息（上下文保持） | ✅ |
| B | 指代消解是否正确（"那个"指什么） | ✅ |
| C | 长对话后是否仍保持角色设定 | ✅ |
| D | 模型的 GPU 显存占用情况 | ❌ |

### 你的错误

选了 ABCD，多选了 D

### 正确解析

正确答案是 **ABC**。

- A/B/C 都属于**多轮对话的功能/质量测试**维度
- D（GPU 显存占用）属于**性能/工程测试**维度，和「多轮对话测试」是两个不同的话题

### 记忆技巧

> **多轮对话测试 = 对话质量**（内容层面）：上下文、指代、角色、话题切换、边界
> **性能测试 = 资源消耗**（工程层面）：GPU、延迟、吞吐、并发
> 
> 题目问的是「多轮对话测试关注什么」→ 只选对话质量相关的

### 面试延伸

多轮对话测试的完整关注点：
1. **上下文保持**：是否记住前文
2. **指代消解**：代词指向是否正确
3. **角色一致性**：长对话中是否保持人设
4. **话题切换与回归**：换话题后再回到原话题是否一致
5. **长度与截断**：轮次多、上下文过长时是否丢信息
6. **边界**：空回复、重复提问、无效输入

---

## 错题 3：`*args` 和 `**kwargs` 的类型（第三轮·第 1 题）

### 题目

> `*args` 和 `**kwargs` 分别把参数打包成什么类型？

| 选项 | 内容 |
|------|------|
| A | `*args` → 列表，`**kwargs` → 元组 |
| **B** ✅ | **`*args` → 元组，`**kwargs` → 字典** |
| C | `*args` → 字典，`**kwargs` → 列表 |
| D | 两者都是列表 |

### 你的错误

选了 A，把两者记反了

### 正确解析

```python
def demo(*args, **kwargs):
    print(type(args))    # <class 'tuple'>  ← 元组！
    print(type(kwargs))  # <class 'dict'>   ← 字典！
    print(args)          # (1, 2, 3)
    print(kwargs)        # {'a': 1, 'b': 2}

demo(1, 2, 3, a=1, b=2)
```

### 记忆技巧

> **`*args`（arguments）= 位置参数 → 元组（tuple）**
> - 位置参数是按顺序排列的，元组有序且不可变，刚好对应
>
> **`**kwargs`（keyword arguments）= 关键字参数 → 字典（dict）**
> - 关键字参数是 `name=value` 的映射，字典就是键值对映射
>
> 口诀：**「位置排元组，关键找字典」**

---

## 错题 4：浅拷贝代码输出（第三轮·第 4 题）

### 题目

> 以下代码输出什么？

```python
a = [1, [2, 3]]
import copy
b = copy.copy(a)
b[1].append(4)
print(a)
```

| 选项 | 内容 |
|------|------|
| A | `[1, [2, 3]]` |
| **B** ✅ | **`[1, [2, 3, 4]]`** |
| C | `[1, [2, 3], 4]` |
| D | 报错 |

### 你的错误

选了 C（`[1, [2, 3], 4]`），混淆了 `b[1].append(4)` 和 `b.append(4)` 的区别

### 正确解析

**浅拷贝只复制第一层**，嵌套的对象仍然是引用（指向同一个内存地址）：

```
浅拷贝后的内存状态：

a ──→ [ 1,  ───→ [2, 3] ]
                   ↑
b ──→ [ 1,  ───→──┘     ]   ← a[1] 和 b[1] 指向同一个 [2, 3]
```

所以 `b[1].append(4)` 修改的是**那个共享的内层列表**，`a[1]` 也跟着变：

```python
b[1].append(4)     # 在共享的 [2,3] 后面加了 4
print(a)           # [1, [2, 3, 4]]  ← a 也受影响
print(b)           # [1, [2, 3, 4]]
```

**注意区分：**
- `b[1].append(4)` → 修改 b 的第二个元素（内层列表）→ **影响 a**
- `b.append(4)` → 在 b 外层列表末尾加元素 → **不影响 a**

### 如果用深拷贝呢？

```python
b = copy.deepcopy(a)   # 递归复制所有层级
b[1].append(4)
print(a)               # [1, [2, 3]]  ← a 不受影响！
```

### 记忆技巧

> **浅拷贝 = 只复制「壳」，里面的东西还是共享的**
> **深拷贝 = 连「壳」带「馅」全部复制一份新的**
>
> 面试场景：测试数据有嵌套结构时，用例之间用 `deepcopy` 隔离，避免互相污染

---

## 错题 5：Docker Compose `depends_on` 的限制（第三轮·第 9 题）

### 题目

> Docker Compose 中 `depends_on` 的作用和限制？

| 选项 | 内容 |
|------|------|
| A | 保证依赖服务完全启动并就绪后，再启动当前服务 |
| **B** ✅ | **只保证依赖服务"已启动"（容器已运行），不保证服务"就绪"（如数据库可连接）** |
| C | 控制容器的 CPU 优先级 |
| D | 限制容器之间的网络访问 |

### 你的错误

选了 A，以为 `depends_on` 会等服务完全就绪

### 正确解析

这是一个**非常经典的坑**，很多人踩过：

```yaml
services:
  app:
    depends_on:
      - db       # 只保证 db 容器先 start，不等 MySQL 初始化完成！
  db:
    image: mysql:8
```

**实际情况：**
1. Docker 先启动 `db` 容器 ✅
2. MySQL 容器启动了，但数据库引擎还在初始化（需要几秒） ⏳
3. Docker 看到 `db` 容器已经 running，就启动 `app` ✅
4. `app` 尝试连接 MySQL → **连接失败！** ❌

### 解决方案

**方案一：应用层重试（推荐）**

```python
# 应用启动时做连接重试
import time
for i in range(30):
    try:
        db.connect()
        break
    except:
        time.sleep(1)
```

**方案二：healthcheck + condition（Compose v2）**

```yaml
services:
  db:
    image: mysql:8
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 3s
      retries: 10
  app:
    depends_on:
      db:
        condition: service_healthy   # 等 db 健康检查通过才启动
```

**方案三：wait-for-it 脚本**

```yaml
services:
  app:
    command: ["./wait-for-it.sh", "db:3306", "--", "python", "app.py"]
```

### 记忆技巧

> **`depends_on` = 只管"启动顺序"，不管"就绪状态"**
> 
> 口诀：**「depends_on 管先后，不管死活」**
>
> 要等就绪 → 用 healthcheck + condition 或应用层重试

---

---

## 错题 6：Python 可变默认参数陷阱（第四轮·第 1 题）

### 题目

> 以下代码的输出是什么？

```python
def add_item(item, lst=[]):
    lst.append(item)
    return lst

print(add_item(1))
print(add_item(2))
```

| 选项 | 内容 |
|------|------|
| A | `[1]` 然后 `[2]` |
| **B** ✅ | **`[1]` 然后 `[1, 2]`** |
| C | `[1, 2]` 然后 `[1, 2]` |
| D | 报错 |

### 你的错误

选了 A，以为每次调用都会创建新列表

### 正确解析

Python 的**可变默认参数只在函数定义时创建一次**，之后每次调用都共享同一个对象：

```python
def add_item(item, lst=[]):  # 这个 [] 只创建一次！
    lst.append(item)
    return lst

print(add_item(1))  # [1]       ← 第一次，lst = [1]
print(add_item(2))  # [1, 2]    ← 第二次，还是同一个 lst，变成 [1, 2]
```

**正确写法：**

```python
def add_item(item, lst=None):
    if lst is None:
        lst = []       # 每次调用时创建新列表
    lst.append(item)
    return lst

print(add_item(1))  # [1]
print(add_item(2))  # [2]  ← 现在互不影响了
```

### 记忆技巧

> **可变对象（list/dict/set）不要做默认参数！用 `None` 代替**
>
> 口诀：**「默认参数别用可变，要用就传 None」**
>
> 这是 Python 面试中**出现频率最高的坑之一**

### 面试延伸

- 面试官可能追问：「为什么会这样？」
- 答：函数定义时，默认参数的值就被**求值并绑定**了，之后每次调用不会重新创建，所以可变对象会在调用间共享状态

---

## 错题 7：CI 流水线中敏感信息管理（第四轮·第 8 题）

### 题目

> 在 CI 流水线中管理敏感信息（密码、API Key），正确的做法是？

| 选项 | 内容 | 正确？ |
|------|------|--------|
| A | 直接写在代码或配置文件里 | ❌ 最常见的安全事故 |
| B | 写在 commit message 里备注 | ❌ Git 历史永久可见 |
| **C** ✅ | **用平台的 Secrets/Credentials/Variables 管理，不写明文** | ✅ |
| D | 用注释写在 Jenkinsfile 里 | ❌ 注释也是代码的一部分 |

### 你的错误

选了 B，以为 commit message 里比较安全

### 正确解析

**commit message 会永久记录在 Git 历史中**，即使后来修改或删除，通过 `git log` 或 GitHub 历史都能找回，极度危险。

**各平台正确做法：**

| 平台 | 管理方式 | 引用方式 |
|------|----------|----------|
| **Jenkins** | Credentials 插件，界面配置 | `credentials('xxx')` |
| **GitLab CI** | Settings → Variables（Masked/Protected） | `$VARIABLE_NAME` |
| **GitHub Actions** | Settings → Secrets | `${{ secrets.XXX }}` |

### 记忆技巧

> **密码密钥绝对不能出现在以下任何地方：**
> - 代码文件 ❌
> - 配置文件 ❌
> - commit message ❌
> - 注释 ❌
>
> **只能放在平台的加密 Secrets/Credentials 里** ✅
>
> 口诀：**「密钥不落地，只存平台里」**

---

---

## 错题 8：ACID 中的「I」含义（第六轮·第 6 题）

### 题目

> 数据库事务的 ACID 特性中，「I」代表什么？

| 选项 | 内容 | 正确？ |
|------|------|--------|
| A | Integrity（完整性） | ❌ 这是数据库约束的概念，不属于 ACID |
| **B** ✅ | **Isolation（隔离性）** | ✅ |
| C | Index（索引） | ❌ |
| D | Implementation（实现） | ❌ |

### 你的错误

选了 A（Integrity），混淆了「完整性约束」和「事务隔离性」

### 正确解析

**ACID 四大特性：**

| 字母 | 英文 | 中文 | 含义 |
|------|------|------|------|
| A | Atomicity | 原子性 | 事务要么全做，要么全不做 |
| C | Consistency | 一致性 | 事务前后数据满足约束和规则 |
| I | **Isolation** | **隔离性** | 多个事务并发时互不干扰 |
| D | Durability | 持久性 | 事务提交后数据永久保存 |

### 记忆技巧

> 口诀：**「原一隔持」—— 原子、一致、隔离、持久**
>
> Integrity 是约束，Isolation 是事务，ACID 的 I 是**事务**的隔离

---

## 错题 9：TCP 三次握手顺序（第九轮·第 3 题）

### 题目

> TCP 三次握手的正确顺序是？

| 选项 | 内容 | 正确？ |
|------|------|--------|
| A | 客户端发 ACK → 服务端发 SYN → 客户端发 SYN+ACK | ❌ 顺序和角色都反了 |
| **B** ✅ | **客户端发 SYN → 服务端发 SYN+ACK → 客户端发 ACK** | ✅ |
| C | 服务端发 SYN → 客户端发 ACK → 服务端发 SYN+ACK | ❌ |
| D | 客户端发 FIN → 服务端发 ACK → 连接建立 | ❌ FIN 是断开连接用的 |

### 你的错误

选了 A，把三次握手的顺序和发送方搞反了

### 正确解析

```
第一次握手：客户端 --SYN--> 服务端      "我想连接你"
第二次握手：服务端 --SYN+ACK--> 客户端  "收到，我也同意"
第三次握手：客户端 --ACK--> 服务端      "确认，连接建立"
```

**关键区分：**
- **SYN**（Synchronize）= 请求同步/建立连接
- **ACK**（Acknowledge）= 确认收到
- **FIN**（Finish）= 请求断开连接（用于**四次挥手**，不是三次握手）

### 记忆技巧

> 口诀：**「SYN → SYN+ACK → ACK」**
>
> 生活化记忆：**「你好 → 你好+收到 → 收到」**
>
> - 客户端先说「你好」（SYN）
> - 服务端回「你好 + 我收到了」（SYN+ACK）
> - 客户端确认「收到」（ACK）
> - 三次确认后双方都知道对方能收发消息，连接建立

### 面试延伸

- 面试官可能追问：「为什么是三次而不是两次？」
- 答：两次握手无法确认客户端的**接收能力**，可能导致服务端为已失效的连接请求分配资源（如客户端发了 SYN 后宕机，服务端以为连接已建立）

---

## 总结：九道错题速览表

| 编号 | 轮次 | 题目关键词 | 你的答案 | 正确答案 | 一句话记忆 |
|------|------|-----------|----------|----------|-----------|
| 1 | 二 | 单例重写哪个方法 | `__init__` | **`__new__`** | new 造房子，init 装修，单例管造不造 |
| 2 | 二 | 多轮对话测试关注点 | ABCD | **ABC** | 对话质量 ≠ 性能指标，GPU 是性能维度 |
| 3 | 三 | args/kwargs 类型 | 列表/元组 | **元组/字典** | 位置排元组，关键找字典 |
| 4 | 三 | 浅拷贝行为 | `[1,[2,3],4]` | **`[1,[2,3,4]]`** | 浅拷贝只复制壳，里面的还是共享 |
| 5 | 三 | depends_on 限制 | 等就绪 | **只等启动** | depends_on 管先后，不管死活 |
| 6 | 四 | 可变默认参数 | `[1]`和`[2]` | **`[1]`和`[1,2]`** | 默认参数别用可变，要用就传 None |
| 7 | 四 | CI 敏感信息管理 | commit 里写 | **平台 Secrets** | 密钥不落地，只存平台里 |
| 8 | 六 | ACID 的 I | Integrity | **Isolation** | 原一隔持，I 是隔离不是完整 |
| 9 | 九 | TCP 三次握手 | ACK→SYN→SYN+ACK | **SYN→SYN+ACK→ACK** | 你好→你好+收到→收到 |

---

## 考前速记口诀

```
new 造房 init 装修，单例重写 new
位置排元组，关键找字典
浅拷贝只复制壳，深拷贝连馅一起
对话测试看质量，GPU 归性能管
depends_on 管先后，就绪要靠 healthcheck
默认参数别用可变，要用就传 None
密钥不落地，只存平台里
原一隔持，ACID 的 I 是 Isolation
三次握手：SYN → SYN+ACK → ACK（你好→你好收到→收到）
```

---

*本文件由九轮模拟面试错题整理而成，建议面试前一天再过一遍。*
*搭配《09-高频场景题-面试官追问精选.md》和各专题面试题文件一起复习效果更好。*
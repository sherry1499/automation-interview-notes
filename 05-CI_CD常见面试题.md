# CI/CD 常见面试题总结

> 适用于测试开发 / DevOps / 质量保障岗位面试准备

---

## 一、CI/CD 概念与区别

### 1. CI、CD 分别是什么？有什么区别？

- **CI（Continuous Integration，持续集成）**：开发者频繁将代码合并到主干，每次合并后**自动触发构建与测试**（编译、单元测试、接口测试等），尽早发现集成错误，保证主干可构建、可测。
- **CD 有两种常见含义**：
  - **持续交付（Continuous Delivery）**：在 CI 通过后，代码**随时处于可发布状态**，部署到生产需要**人工审批或触发**。
  - **持续部署（Continuous Deployment）**：在 CI 通过后，**自动部署到生产**，无需人工操作（通常仍有审批流程或灰度）。
- **关系**：CI 是基础；CD 建立在 CI 之上，强调「可发布」或「自动发布」。

**面试点**：CI = 频繁集成 + 自动构建测试；CD = 可交付/可部署，区别在于是否自动上生产、是否需人工审批。

---

### 2. 为什么要做 CI/CD？解决了什么问题？

- **尽早发现问题**：代码一提交就测，缺陷在开发阶段暴露，修复成本低。
- **减少手工与人为失误**：构建、测试、部署自动化，减少漏步骤、环境不一致。
- **加快反馈与发布**：从「周/月发布」到「日/多次发布」，提升交付效率。
- **可追溯与可回滚**：每次构建/部署有记录，出问题可快速定位与回滚。
- **质量门禁**：测试不通过不能合并或发布，保证主干质量。

**面试点**：左移、自动化、快速反馈、质量门禁、可追溯。

---

### 3. DevOps 和 CI/CD 的关系？

- **DevOps**：开发与运维协作的文化与实践，强调**自动化、协作、快速迭代、监控与反馈**；CI/CD 是 DevOps 在「构建—测试—部署」上的**核心实践与工具链**。
- **关系**：CI/CD 是 DevOps 的**重要实现手段**；DevOps 还包含基础设施即代码（IaC）、监控、日志、安全等更广范畴。

**面试点**：DevOps 是理念与文化，CI/CD 是其中关键的自动化流水线实践。

---

## 二、持续集成实践

### 4. 典型的 CI 流程包含哪些阶段？

1. **代码拉取**：从 Git 拉取最新代码（含 PR/merge 触发）。
2. **依赖安装**：安装语言/框架依赖（npm install、pip install、mvn 等）。
3. **构建/编译**：编译、打包、构建镜像等。
4. **静态检查**：代码规范、安全扫描（SonarQube、ESLint、pylint 等）。
5. **测试**：单元测试、接口测试、关键 UI 测试等。
6. **产物**：生成包/镜像，归档或推送到仓库。
7. **通知**：成功/失败通知（邮件、企微、钉钉等）；失败可阻断合并或部署。

**面试点**：拉代码 → 依赖 → 构建 → 检查 → 测试 → 产物 → 通知；测试失败即阻断。

---

### 5. 什么是质量门禁？在 CI 中如何实现？

- **质量门禁**：在合并或发布前设置**必须满足的条件**，不满足则不允许合并/发布，如：构建成功、测试通过、覆盖率达标、无高危漏洞等。
- **实现**：在流水线中把「测试阶段」「覆盖率检查」「安全扫描」等设为**必须通过**；失败则整个流水线失败，PR 不能合并；部分平台支持 Branch protection 要求 CI 通过才能 merge。

**面试点**：门禁 = 不达标则不放行；实现 = 流水线阶段失败即阻断 + 分支保护规则。

---

### 6. 如何在 CI 中集成自动化测试？

- **触发**：代码 push/PR 时触发流水线，或定时触发（如每日回归）。
- **步骤**：拉代码 → 准备环境（虚拟环境/Docker/Node 等）→ 安装依赖 → 执行测试命令（pytest、mvn test、npm test 等）→ 收集结果（JUnit XML、Allure、覆盖率报告）。
- **结果**：测试失败则阶段失败，流水线标红；报告归档或上传到平台（如 Allure Server、Jenkins 插件）便于查看。
- **环境**：使用固定 Agent 或 Docker 镜像保证环境一致，避免「本地能过、CI 不过」。

**面试点**：触发 → 环境 → 执行 → 结果收集与归档 → 失败阻断；环境一致性很重要。

---

## 三、常用 CI/CD 工具

### 7. Jenkins、GitLab CI、GitHub Actions 的区别与选型？

| 对比项       | Jenkins              | GitLab CI / GitLab Pipelines | GitHub Actions        |
|--------------|----------------------|------------------------------|------------------------|
| 部署方式     | 自建，需维护服务器   | 自建或 GitLab.com 托管       | 托管在 GitHub，按用量 |
| 配置方式     | 界面 / Jenkinsfile   | .gitlab-ci.yml               | .github/workflows/*.yml |
| 与代码仓库   | 可对接多种 Git       | 与 GitLab 深度集成           | 与 GitHub 深度集成     |
| 生态         | 插件多、灵活         | 与 GitLab 一体化             | 与 GitHub 一体化、Marketplace |
| 适用         | 传统企业、多仓库统一 | GitLab 用户、私有化           | GitHub 用户、开源/云上 |

**面试点**：Jenkins 自建灵活；GitLab CI 跟 GitLab 走；GitHub Actions 跟 GitHub 走、托管省心。

---

### 8. Jenkins Pipeline 是什么？声明式与脚本式区别？

- **Pipeline**：用**代码**定义流水线（Jenkinsfile），可版本管理、可复用，支持多阶段、并行、条件判断。
- **声明式（Declarative）**：用固定语法（stages、steps、post 等），结构清晰、易读，适合大多数场景。
- **脚本式（Scripted）**：用 Groovy 脚本，灵活度更高，适合复杂逻辑，但可读性稍差。
- **常用**：声明式 Pipeline + Jenkinsfile 放在项目仓库中。

**面试点**：Pipeline = 流水线即代码；声明式简单规范，脚本式灵活。

---

### 9. GitHub Actions 的核心概念？workflow、job、step？

- **Workflow**：一个 YAML 文件定义的一次自动化流程，由事件（push、pull_request、schedule 等）触发。
- **Job**：Workflow 中的一组 **step**，在同一 runner 上顺序执行；多个 job 可并行或按 needs 依赖执行。
- **Step**：Job 内的一条条操作，如 checkout、run 命令、使用 action（可复用脚本/插件）。
- **关系**：Workflow 包含多个 Job，Job 包含多个 Step；Step 可用 `uses` 调用他人写的 action，或用 `run` 执行 shell。

**面试点**：Workflow 由事件触发 → 含多个 Job → 每个 Job 含多个 Step；action 是可复用单元。

---

### 10. GitLab CI 的 .gitlab-ci.yml 常用关键字？

- **stages**：定义阶段顺序，如 build、test、deploy。
- **job**：每个 job 属于某个 stage，写 script、image、only/except 等。
- **script**：该 job 要执行的命令。
- **image**：用 Docker 镜像作为运行环境。
- **cache**：缓存依赖目录，加速后续 job。
- **artifacts**：保存构建/测试产物，供后续 stage 使用或下载。
- **only / rules**：控制何时运行该 job（分支、标签、MR 等）。

**面试点**：stages + job + script 是主骨架；image、cache、artifacts 用于环境与效率。

---

## 四、流水线设计与实践

### 11. 流水线中如何管理敏感信息（密码、密钥）？

- **原则**：**不要**把密码、API Key 写进代码或明文写在配置里。
- **做法**：
  - **Jenkins**：Credentials 插件，在界面配置后通过变量引用（如 `credentials('xxx')`）。
  - **GitLab CI**：Variables（Masked/Protected），在项目/群组设置中配置。
  - **GitHub Actions**：Secrets，在仓库 Settings 中配置，workflow 里用 `${{ secrets.XXX }}`。
- **最佳实践**：敏感变量标记为 Masked，仅在必要分支/环境使用；生产环境单独管理、最小权限。

**面试点**：用平台提供的 Credentials/Secrets/Variables，不写明文；Masked + 最小权限。

---

### 12. 流水线如何做缓存以加快构建？

- **依赖缓存**：把依赖目录（node_modules、venv、Maven .m2 等）在 job 间缓存；命中缓存则跳过或减少安装时间。
- **实现**：
  - **Jenkins**：Pipeline 里用 stash/unstash 或插件缓存指定路径。
  - **GitLab CI**：`cache: paths: [...]`，按 key 区分分支或文件 hash。
  - **GitHub Actions**：`actions/cache`，按 key 和 path 缓存。
- **注意**：缓存 key 要合理（如 lock 文件 hash），避免依赖变了仍用旧缓存；设置过期或大小限制。

**面试点**：缓存依赖目录、key 与依赖变更关联、注意失效与清理。

---

### 13. 多环境（开发 / 测试 / 预发 / 生产）如何设计流水线？

- **思路**：同一流水线通过**参数或分支**区分环境，或不同分支/标签触发不同阶段。
- **常见**：develop 分支 → 自动部署到开发/测试环境；release 或 tag → 部署预发，人工审批后部署生产；或仅生产部署需手动触发 + 审批。
- **配置分离**：环境相关配置（URL、密钥）用环境变量或配置中心，不写死在代码里。
- **门禁**：生产部署前要求测试/预发阶段通过，且可有审批节点。

**面试点**：按分支/标签/参数区分环境；生产加审批与门禁；配置与代码分离。

---

### 14. 流水线失败如何排查？常见原因？

- **看日志**：失败 stage 的完整日志，定位报错行。
- **常见原因**：
  - **环境**：依赖未安装、版本不一致、路径不对 → 检查镜像/Agent、依赖安装步骤。
  - **权限**：拉代码、推镜像、访问密钥失败 → 检查 Credentials、网络、权限。
  - **测试失败**：用例失败或 flaky → 看测试报告，本地复现，修用例或环境。
  - **资源**：磁盘满、内存 OOM、超时 → 清理缓存、调大资源或拆分 job。
- **本地复现**：用相同命令、相同环境（如 Docker）在本地跑一遍，便于调试。

**面试点**：先看失败 stage 日志 → 环境/权限/用例/资源几类原因 → 本地复现。

---

## 五、测试在 CI/CD 中的位置

### 15. 测试左移是什么？在 CI 中如何体现？

- **测试左移**：在研发**早期**就引入测试（需求/设计阶段考虑可测性，开发阶段写单测、做接口测试），而不是等到测试阶段才测，从而更早发现问题、降低成本。
- **在 CI 中体现**：代码一提交就跑单元测试、接口测试；PR 阶段就跑自动化，合并前必须通过；关键路径在开发环境或测试环境自动回归。

**面试点**：左移 = 尽早测；CI = 每次提交都跑自动化，合并前门禁。

---

### 16. 单元测试、接口测试、UI 测试在流水线中如何安排？

- **单元测试**：**必跑、最先**，速度快、反馈快，失败即阻断；一般在「构建」之后立即执行。
- **接口测试**：**必跑**，覆盖核心接口与契约；可紧跟单元测试或单独 stage；需要稳定环境或 Docker 起服务。
- **UI 测试**：**选跑**或**定时跑**，耗时长、易 flaky；可在 PR 时跑少量冒烟，全量放定时任务（如每日）或发布前流水线。
- **顺序**：拉代码 → 构建 → 单测 → 接口测试 →（可选）UI 冒烟 → 产物/部署；失败即停，不执行后续。

**面试点**：单测必跑且靠前；接口必跑；UI 可冒烟或定时，避免阻塞主流水线。

---

### 17. 如何把测试报告展示在 CI 中？（如 Allure、JUnit）

- **产出标准格式**：测试框架生成 JUnit XML、Allure 结果等，流水线里指定输出路径。
- **Jenkins**：JUnit Plugin、Allure Plugin 等，在 job 中配置「归档/发布」该路径，构建后可在界面查看报告与趋势。
- **GitLab CI**：artifacts 上传报告文件；或集成 Allure、Jest 等报告，用 artifacts 或外部服务展示。
- **GitHub Actions**：上传 artifacts；或使用 action 将结果发到 Allure Server、TestRail 等；也有 action 在 PR 上评论测试结果摘要。

**面试点**：统一输出格式 → 流水线归档或上传 → 用插件或平台展示报告与趋势。

---

## 六、持续交付与部署

### 18. 蓝绿部署、金丝雀（灰度）部署是什么？

- **蓝绿部署**：两套**完整环境**（蓝、绿），当前流量在蓝；新版本部署到绿，验收通过后**切换流量**到绿，蓝保留可快速回滚。优点切换快、回滚简单；缺点资源双倍。
- **金丝雀/灰度**：新版本先给**一小部分**用户或流量，观察指标与错误率，无问题再全量。可配合按比例、按用户、按地域放量。
- **在 CI/CD 中**：流水线负责构建与部署到对应环境；流量切换可能由发布平台、Ingress、网关等完成，不一定在流水线里。

**面试点**：蓝绿 = 双环境 + 流量切换；金丝雀 = 小流量验证再全量。

---

### 19. 部署失败或上线后发现问题，如何回滚？

- **回滚方式**：重新部署**上一版本**的镜像或包；或通过蓝绿切换回原环境；或数据库/配置有变更时需有回滚脚本或备份恢复。
- **前提**：每次部署有**版本记录**（镜像 tag、包版本）；流水线或发布平台支持「一键回滚到上一版本」。
- **测试参与**：制定回滚预案与检查清单；必要时做回滚演练，确保流程可用。

**面试点**：可追溯版本 + 一键回滚到上一版本；数据库/配置需单独考虑。

---

## 七、小结速记

| 模块       | 关键词 |
|------------|--------|
| 概念       | CI=集成+自动构建测试，CD=持续交付/部署，DevOps 与 CI/CD 关系 |
| 流程       | 拉代码→依赖→构建→检查→测试→产物→通知，质量门禁 |
| 工具       | Jenkins（Pipeline/Jenkinsfile），GitLab CI（.gitlab-ci.yml），GitHub Actions（workflow/job/step） |
| 实践       | 敏感信息用 Secrets/Credentials，缓存依赖，多环境与审批，失败排查 |
| 测试       | 测试左移，单测/接口/UI 在流水线中的安排，报告展示 |
| 发布       | 蓝绿、金丝雀、回滚与版本追溯 |

---

*可与《Python自动化测试常见面试题》《测试开发常见面试题》《Docker常见面试题》等搭配复习，CI/CD 是测试开发与 DevOps 的常考项。*

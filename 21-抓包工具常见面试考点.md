# 抓包工具常见面试考点（Charles / Fiddler）

> 适用于测试开发 / 接口测试 / APP 测试岗位面试准备

---

## 一、抓包基础概念

### 什么是抓包？

> 抓包是指拦截和查看客户端与服务器之间的网络请求和响应数据，用于**接口调试、Bug 定位、安全测试**。

### 抓包原理

```
正常通信：
  客户端 ←————————→ 服务器

抓包时（中间人代理）：
  客户端 ←→ 代理（Charles/Fiddler）←→ 服务器
```

> **核心原理**：抓包工具作为**中间人代理（Man-in-the-Middle）**，客户端的请求先到代理，代理转发给服务器；服务器的响应先到代理，代理再转发给客户端。

---

## 二、Charles vs Fiddler 对比

| 对比项 | Charles | Fiddler |
|--------|---------|---------|
| 平台 | **Mac / Windows / Linux** | 主要 **Windows**（Fiddler Everywhere 跨平台） |
| 收费 | 收费（有试用） | 经典版免费 |
| 界面 | 简洁美观 | 功能密集 |
| HTTPS 抓包 | ✅ 安装证书 | ✅ 安装证书 |
| 手机抓包 | ✅ 设置代理 | ✅ 设置代理 |
| 弱网模拟 | ✅ Throttle | ✅ Rules → Performance |
| 断点修改 | ✅ Breakpoints | ✅ Breakpoints |
| Map 功能 | ✅ Map Local / Map Remote | ✅ AutoResponder |
| 推荐使用 | Mac 用户首选 | Windows 用户首选 |

---

## 三、HTTPS 抓包配置（必考）

### 为什么 HTTPS 默认抓不了？

> HTTPS 通过 SSL/TLS 加密，代理无法解密内容。需要安装抓包工具的**根证书（CA证书）**，让客户端信任代理签发的证书。

### 配置步骤

```
1. 抓包工具开启 SSL Proxy
   - Charles：Proxy → SSL Proxying Settings → 添加 *:443
   - Fiddler：Tools → Options → HTTPS → Decrypt HTTPS traffic

2. 安装根证书
   - 电脑：Help → SSL Proxying → Install Root Certificate
   - 手机：浏览器访问 chls.pro/ssl（Charles）下载安装证书

3. 手机信任证书
   - Android：设置 → 安全 → 信任的凭据（Android 7+ 需要额外配置）
   - iOS：设置 → 通用 → 关于 → 证书信任设置 → 开启

4. 手机设置代理
   - Wi-Fi → 手动代理 → 输入电脑 IP + 端口（Charles 默认 8888）
```

### Android 7+ 抓包问题（常考）

> Android 7.0 以后，App 默认**不信任用户安装的证书**，只信任系统证书。

**解决方案**：
1. 使用 Android 7 以下的模拟器
2. Root 后将证书安装到系统目录
3. 反编译 APK，修改 `network_security_config.xml` 允许用户证书
4. 使用 Xposed/VirtualXposed + JustTrustMe 模块绕过

---

## 四、手机抓包配置（APP 测试必会）

### 步骤

```
1. 电脑和手机连接同一 Wi-Fi
2. 电脑上打开 Charles/Fiddler，查看电脑 IP
   - Charles：Help → Local IP Address
3. 手机设置代理
   - Wi-Fi 设置 → 手动代理
   - 服务器：电脑 IP（如 192.168.1.100）
   - 端口：8888（Charles）/ 8866（Fiddler）
4. 手机浏览器访问证书下载地址（HTTPS 需要）
   - Charles：chls.pro/ssl
   - Fiddler：ipv4.fiddler:8866
5. 安装并信任证书
6. 开始抓包
```

---

## 五、核心功能（面试常问）

### 1. 断点（Breakpoints）— 修改请求/响应

```
场景：修改请求参数或响应数据，验证前端/后端处理逻辑

Charles：
  - 右键请求 → Breakpoints
  - 请求到达时会暂停，可修改参数后再发送
  - 支持对 Request 和 Response 分别断点

Fiddler：
  - Rules → Automatic Breakpoints → Before Requests / After Responses
  - 或在命令栏输入 bpu <url> 对特定请求断点
```

**面试常见场景**：
- 修改响应状态码（如把 200 改成 500）→ 测试前端异常处理
- 修改响应数据（如把余额改成负数）→ 测试边界情况
- 修改请求参数 → 测试后端参数校验

### 2. Map Local / Map Remote — 接口 Mock

```
Map Local：用本地文件替代服务器响应
  - 右键请求 → Map Local → 选择本地 JSON 文件
  - 场景：后端接口没开发好，用本地文件模拟返回

Map Remote：将请求转发到另一个地址
  - 右键请求 → Map Remote → 输入新的 URL
  - 场景：将生产环境请求转到测试环境
```

### 3. Rewrite — 批量修改

```
Charles：Tools → Rewrite
  - 批量修改 Header、Body、状态码等
  - 可以添加/删除/修改指定字段

场景：批量给所有请求加一个自定义 Header
```

### 4. Repeat / Compose — 重放请求

```
Charles：
  - Repeat：重发请求（可设置次数 → 简单压测）
  - Compose：编辑后重发（修改参数后再发送）

Fiddler：
  - Replay：重发请求
  - Composer：手动构造请求
```

### 5. Throttle — 弱网模拟（常考）

```
Charles：Proxy → Throttle Settings
  - 预设：56kbps / 3G / 4G 等
  - 自定义：带宽、延迟、丢包率

Fiddler：Rules → Performance → Simulate Modem Speeds

测试场景：
  - 弱网下页面加载时间
  - 弱网下请求超时处理
  - 弱网下图片/视频加载体验
  - 网络切换时的数据一致性
```

---

## 六、抓包在测试中的实际应用

| 应用场景 | 具体操作 |
|---------|---------|
| **接口调试** | 查看请求参数和响应数据，验证接口是否正确 |
| **Bug 定位** | 通过抓包判断是前端 Bug 还是后端 Bug |
| **Mock 数据** | Map Local 模拟返回，不依赖后端 |
| **弱网测试** | Throttle 模拟 2G/3G/断网场景 |
| **安全测试** | 篡改请求参数（如价格、数量），验证后端校验 |
| **性能分析** | 查看接口响应时间、数据大小 |
| **重复请求** | Repeat 功能检测接口幂等性 |

### 前端 Bug vs 后端 Bug 定位（高频场景题）

```
步骤：
1. 页面出现异常 → 打开抓包工具
2. 查看对应接口的请求和响应
3. 判断逻辑：
   - 请求参数错误 → 前端 Bug（前端传错了）
   - 请求正确但响应数据错误 → 后端 Bug
   - 响应正确但页面显示错误 → 前端 Bug（前端渲染问题）
   - 请求根本没发出去 → 前端 Bug
   - 返回 500 等错误码 → 后端 Bug
```

---

## 七、常见面试题及参考答案

### Q1：抓包的原理是什么？

> 抓包工具充当**中间人代理**，客户端请求先到代理，代理转发给服务器；响应也经过代理中转。通过拦截中间流量来查看和修改数据。

### Q2：HTTPS 为什么需要安装证书才能抓包？

> HTTPS 通过 SSL/TLS 加密，代理无法解密。安装证书后，客户端信任代理签发的证书，代理分别与客户端和服务端建立 SSL 连接，实现解密。

### Q3：Android 7+ 为什么抓不到 HTTPS？怎么解决？

> Android 7+ 默认不信任用户证书。解决：用低版本模拟器、Root 安装系统证书、修改 APK 的网络安全配置、或用 JustTrustMe 模块。

### Q4：如何通过抓包判断是前端还是后端的 Bug？

> 看请求参数是否正确（前端问题）→ 看响应数据是否正确（后端问题）→ 看页面渲染是否正确（前端问题）。

### Q5：什么是弱网测试？怎么做？

> 模拟网络差的环境（2G/3G/高延迟/丢包）进行测试。用 Charles Throttle 或 Fiddler 模拟弱网，观察加载时间、超时处理、数据一致性。

### Q6：Map Local 有什么用？

> 用本地文件替代服务器返回的响应，用于接口 Mock。场景：后端没开发好，前端先用模拟数据联调。

### Q7：如何用抓包工具做简单的接口安全测试？

> 用断点拦截请求 → 修改参数（如价格、权限、用户 ID）→ 放行 → 观察服务端是否校验。如果篡改后请求成功，说明存在安全漏洞。

---

## 速记口诀

```
抓包原理 = 中间人代理
HTTPS 抓包 = 安装证书 + 信任证书
手机抓包 = 同一 Wi-Fi + 设置代理 + 装证书

Charles 端口 8888，Fiddler 端口 8866
Android 7+ 不信任用户证书 → 解决方案四选一

断点：改请求改响应，测前端测后端
Map Local：本地 Mock，不依赖后端
Throttle：弱网模拟，测超时测加载

Bug 定位三步：看请求 → 看响应 → 看页面
请求错 = 前端，响应错 = 后端，渲染错 = 前端
```

---

*可与《17-网络基础知识常见面试考点.md》《02-Python自动化测试常见面试题.md》搭配复习，抓包是接口测试和 APP 测试的必备技能。*

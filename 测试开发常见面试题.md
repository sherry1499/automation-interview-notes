# 测试开发常见面试题总结

> 适用于测试开发（SDET）/ 测试架构岗位面试准备

---

## 一、测试基础

### 1. 测试开发与普通测试工程师的区别？

| 对比项       | 测试工程师           | 测试开发                     |
|--------------|----------------------|------------------------------|
| 职责         | 手工/用例/缺陷       | 自动化、工具、平台、效能     |
| 编程         | 会写脚本即可         | 需扎实编码与设计能力         |
| 产出         | 测试报告、用例       | 框架、平台、流水线、质量体系 |
| 协作         | 与开发/产品          | 与开发/运维/平台/工具链      |

**面试点**：测试开发 = 测试思维 + 开发能力，偏向「用技术提升质量与效率」。

---

### 2. 黑盒、白盒、灰盒测试的区别？

- **黑盒**：不关心内部实现，只看输入输出、规格说明（等价类、边界值、场景法等）。
- **白盒**：基于代码结构，覆盖语句/分支/路径（单元测试、覆盖率）。
- **灰盒**：结合两者，如接口测试既看请求响应，又结合代码/日志排查。

**面试点**：测试开发常做灰盒（接口 + 代码/日志）+ 自动化。

---

### 3. 测试用例设计方法有哪些？

| 方法           | 说明                     | 典型场景         |
|----------------|--------------------------|------------------|
| 等价类划分     | 有效/无效等价类         | 输入框、参数校验 |
| 边界值分析     | 边界及边界外一点        | 长度、范围、数量 |
| 判定表         | 条件组合 → 动作         | 多条件业务规则   |
| 因果图         | 原因-结果逻辑           | 复杂业务逻辑     |
| 场景法         | 用户流程、主/备/异常流  | 业务流程验收     |
| 正交/配对      | 减少组合数、覆盖关键组合| 多参数组合       |

**面试点**：能说出 3～4 种并举例（如登录：等价类+边界值；下单：场景法）。

---

### 4. 如何评估测试是否充分？覆盖率有哪些？

- **评估维度**：需求覆盖、用例覆盖、代码覆盖、风险覆盖。
- **代码覆盖率类型**：
  - **行覆盖**：语句是否执行。
  - **分支覆盖**：每个判断的 true/false 是否都走到。
  - **条件覆盖**：多条件中每个条件 true/false。
  - **路径覆盖**：所有执行路径（通常不可达 100%）。

**面试点**：高覆盖率不等于无 bug，但要作为「未覆盖区域」的风险参考。

---

### 5. 缺陷严重级别与优先级如何定义？

- **严重程度**（Severity）：对系统/用户的影响——致命、严重、一般、轻微。
- **优先级**（Priority）：修复的紧急程度——P0/P1/P2/P3。
- 关系：严重不一定高优（如影响小但易触发）；高优不一定严重（如阻塞演示）。

**面试点**：能区分两者，并说明在实际项目中如何定级即可。

---

## 二、接口与自动化

### 6. 接口测试主要测什么？与 UI 自动化对比？

- **接口测试**：请求/响应、状态码、数据结构、业务逻辑、异常码、权限、边界。
- **对比**：
  - 接口：稳定、快、易维护、覆盖深，适合回归与 CI。
  - UI：更贴近用户、发现前端/交互问题，成本高、易受界面变更影响。

**面试点**：接口自动化是测试开发的主战场，UI 作为补充或关键流程。

---

### 7. HTTP 状态码常见有哪些？含义？

| 状态码  | 含义           |
|---------|----------------|
| 200     | 成功           |
| 201     | 创建成功       |
| 301/302 | 重定向         |
| 400     | 请求参数错误   |
| 401     | 未认证         |
| 403     | 无权限         |
| 404     | 资源不存在     |
| 500     | 服务器内部错误 |
| 502/503 | 网关/服务不可用 |

**面试点**：接口断言时常用 200/201、4xx/5xx 区分客户端与服务端问题。

---

### 8. 如何设计接口自动化框架？（分层/目录）

- **分层思路**：
  - **用例层**：测试用例、数据驱动（parametrize/CSV）。
  - **业务层**：封装业务接口（如登录、下单），供用例调用。
  - **接口层**：封装 HTTP 客户端（request、鉴权、日志）。
  - **公共层**：配置、日志、数据库/Redis 工具、断言封装。
- **目录示例**：`config/`、`utils/`、`api/`、`testcases/`、`data/`、`report/`。

**面试点**：强调「分层、可复用、数据与代码分离、易维护」。

---

### 9. 接口自动化中如何处理依赖（如登录 token）？

- **方案**：登录接口 → 提取 token（正则/JSON 提取）→ 存为变量或全局 session → 后续请求 header 带 `Authorization: Bearer ${token}`。
- **框架层面**：用 pytest fixture（session 作用域）在会话开始时登录一次，注入到需要鉴权的接口封装里。
- **数据隔离**：多环境/多账号用配置或 CSV 管理，避免硬编码。

**面试点**：能说清「一次登录、全局复用」+ fixture 或等价实现即可。

---

### 10. 你用过哪些接口测试/自动化工具？

- **Postman**：手工调试、Collection 运行、Newman 命令行。
- **JMeter**：接口 + 性能、脚本化、分布式。
- **requests + pytest**：代码化、CI 集成、定制能力强。
- **RestAssured（Java）**：BDD 风格、与 Java 技术栈集成。

**面试点**：结合项目说 1～2 个，并说明选型理由（如 Python 栈用 requests+pytest）。

---

## 三、持续集成与 DevOps

### 11. CI/CD 是什么？测试在其中的位置？

- **CI**（持续集成）：代码提交后自动构建、跑单元测试/接口测试，尽早发现集成问题。
- **CD**（持续交付/部署）：在 CI 通过基础上，自动部署到测试/预发/生产（含流水线审批）。
- **测试位置**：在 CI 流水线中跑自动化（单元、接口、关键 UI），通过才允许合并或发布；有时还有门禁（质量门禁）。

**面试点**：测试左移 + 自动化门禁 + 流水线内嵌测试阶段。

---

### 12. Jenkins / GitLab CI 中如何集成自动化测试？

- **思路**：流水线阶段中增加「测试」步骤，执行测试命令（如 `pytest`、`mvn test`），收集结果（JUnit/Allure 等），失败则阶段失败。
- **常见做法**：拉代码 → 安装依赖 → 执行测试 → 上传报告/归档 → 通知（邮件/企微）。
- **环境**：用 Docker 或专用 Agent 保证环境一致。

**面试点**：能描述「触发 → 执行 → 结果上报 → 失败阻断」的闭环即可。

---

### 13. 如何保证自动化用例的稳定性（ flaky 治理）？

- **原因**：依赖环境/时间/顺序、网络/第三方、未显式等待、数据未隔离。
- **手段**：显式等待、重试机制（pytest-rerunfailures）、用例独立与数据隔离、 mock 不稳定依赖、稳定环境与数据准备、剔除或隔离 flaky 用例并单独治理。

**面试点**：先定位原因（环境/顺序/依赖），再针对性加等待、重试、隔离、mock。

---

## 四、数据库与 SQL

### 14. 测试中常用的 SQL 操作？

- **查询**：SELECT、WHERE、JOIN（INNER/LEFT）、GROUP BY、ORDER BY、LIMIT。
- **校验**：用查询结果做断言（如查订单状态、余额是否一致）。
- **准备/清理数据**：INSERT、UPDATE、DELETE，在用例前后执行（fixture 或脚本）。

**面试点**：能写多表 JOIN、聚合、子查询，并说明在测试中的用途（造数、校验）。

---

### 15. 事务的 ACID 是什么？

- **A**tomicity：原子性，要么全成功要么全回滚。
- **C**onsistency：一致性，数据满足约束和业务规则。
- **I**solation：隔离性，并发事务互不干扰。
- **D**urability：持久性，提交后持久化。

**面试点**：测试支付、订单等场景时，会涉及事务回滚、隔离级别对结果的影响。

---

### 16. 索引是什么？为什么能加速查询？建索引要注意什么？

- **索引**：类似目录，加速 WHERE、JOIN、ORDER BY 的查找。
- **注意**：索引会占空间、写操作会维护索引；过多索引影响写入；区分唯一索引、联合索引、覆盖索引；避免在低区分度列建索引。

**面试点**：测试大数据量或性能时，会关注是否有合适索引、是否走了索引（explain）。

---

## 五、Linux 与网络

### 17. 测试/排查时常用的 Linux 命令？

| 类型     | 命令示例                    |
|----------|-----------------------------|
| 文件     | ls、cat、tail -f、grep、find |
| 进程     | ps、kill、netstat/lsof      |
| 日志     | tail -f、grep、awk、sed     |
| 资源     | top、df、free               |
| 网络     | curl、ping、telnet、nc      |

**面试点**：能说「查日志用 tail+grep」「查端口/进程用 netstat/lsof」等实际用法。

---

### 18. HTTP 与 HTTPS 的区别？

- **HTTPS** = HTTP + TLS/SSL，对传输加密、可做身份验证。
- **区别**：端口（80 vs 443）、安全性、HTTPS 需证书；接口测试时 HTTPS 要处理证书校验（如 requests 里 verify=False 仅测试环境）。

**面试点**：接口/抓包时知道证书、代理、verify 即可。

---

### 19. 抓包工具用过吗？如何抓 HTTPS？

- **工具**：Fiddler、Charles、Chrome DevTools、mitmproxy。
- **HTTPS**：安装根证书到本机/手机，代理解密后再转发；抓包时浏览器/客户端需信任该证书。

**面试点**：用于接口分析、重复请求、篡改参数、定位前后端问题。

---

## 六、编程与算法（测试开发常考）

### 20. 常见数据结构及在测试中的用途？

- **列表/数组**：用例列表、结果收集。
- **字典/Map**：请求头、JSON  body、键值配置。
- **集合**：去重、判断存在。
- **栈/队列**：某些场景下的数据顺序、消息队列模拟。

**面试点**：能说清「用什么结构存什么数据」即可，不必深入实现。

---

### 21. 时间复杂度是什么？O(1)、O(n)、O(log n) 举例？

- **大 O**：表示随规模 n 增长，耗时/空间的量级。
- **O(1)**：数组下标访问、哈希表查找。
- **O(n)**：遍历数组、单层循环。
- **O(log n)**：二分查找、平衡树查找。

**面试点**：能区分常见操作的复杂度，便于在造数、校验时选合适写法。

---

### 22. 手写：判断字符串是否为合法括号序列

```python
def is_valid(s: str) -> bool:
    stack = []
    m = {')': '(', ']': '[', '}': '{'}
    for c in s:
        if c in '([{':
            stack.append(c)
        else:
            if not stack or stack[-1] != m.get(c):
                return False
            stack.pop()
    return len(stack) == 0
```

**面试点**：栈匹配括号，注意空栈和最后栈是否为空。

---

### 23. 手写：二分查找

```python
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        if arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1
```

**面试点**：边界 left<=right、mid 加减 1，避免死循环。

---

## 七、项目与场景题

### 24. 如何从 0 搭建公司/项目的接口自动化？

- **阶段**：选型（语言、框架）→ 搭骨架（分层、配置、日志）→ 封装通用请求与鉴权 → 先覆盖主流程与核心接口 → 数据与环境管理 → 接入 CI → 报告与告警 → 迭代维护与用例分层（冒烟/全量）。
- **要点**：先跑通再铺量、用例稳定可维护、和开发约定接口契约与环境。

**面试点**：体现「规划 + 落地节奏 + 可维护性 + CI」。

---

### 25. 线上出现 bug 但自动化没发现，你会怎么分析？

- **可能原因**：用例未覆盖该场景/分支；数据或环境与线上不一致；用例断言不充分；依赖被 mock 掉；偶发/并发/时序问题未模拟。
- **改进**：补充用例与断言、加强异常与边界场景、引入线上流量回放或生产数据脱敏、做代码/需求覆盖分析、做缺陷回溯与用例补充。

**面试点**：不推责，从「覆盖、数据、环境、断言、场景」几方面分析并给出改进。

---

### 26. 如何推动开发参与单元测试/质量建设？

- **思路**：从收益讲起（减少回归成本、提前发现问题）→ 提供脚手架与规范（示例、覆盖率要求）→ 与 CI 门禁挂钩（覆盖率/必测项）→ 展示数据（缺陷逃逸率、线上问题）→ 培训与答疑。
- **注意**：先易后难、从核心服务开始、和 leader 达成一致。

**面试点**：技术 + 流程 + 数据 + 沟通，而不是单方面要求。

---

### 27. 你做过最有挑战的测试相关项目是什么？

- **建议结构**：背景与目标 → 难点（技术/协作/时间）→ 你的方案与角色 → 结果与数据 → 复盘与收获。
- **可讲方向**：自动化从 0 到 1、性能优化与容量规划、质量门禁与流水线、测试平台/工具开发、线上质量治理。

**面试点**：具体、有结果、能体现技术深度和推动力。

---

## 八、小结速记

| 模块       | 关键词 |
|------------|--------|
| 测试基础   | 黑盒/白盒/灰盒，等价类/边界值/场景法，缺陷等级 |
| 接口自动化 | 分层框架、鉴权 fixture、requests+pytest、稳定性 |
| CI/CD      | 流水线嵌测试、报告、门禁、flaky 治理 |
| 数据库     | SELECT/JOIN、事务 ACID、索引、造数/校验 |
| Linux/网络 | 日志 tail+grep、HTTP 状态码、抓包 HTTPS |
| 编程       | 数据结构、复杂度、栈/二分等手写题 |
| 项目       | 从 0 搭建、漏测分析、推动开发、最有挑战项目 |

---

*可与《Python自动化测试常见面试题》《JMeter自动化测试常见面试题》搭配复习，覆盖测试开发常见考点。*
